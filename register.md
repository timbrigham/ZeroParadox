# Zero Paradox Version Registry

Update this file first on any version bump. README.md Framework table and GUIDE.md Reading Paths are verified against this register.

| Document | Formal Version | Filename | Companion Version | Comp AR | Notes |
|----------|---------------|----------|-------------------|---------|-------|
| ZP-A Lattice Algebra | v1.29 | ZP-A_Lattice_Algebra.pdf | v1.11 | N/— | v1.29: DECISION BATCH REMEDIATION AFTER GATE ROUND 3 (Tim rulings, 2026-09-15): each face of ε₀ carries its own direction. The OQ-A1a second paragraph and the minimal-element note said nothing below ε₀ fires because ε₀ is at once the supremum and the least fixed point; the downward direction uses the supremum face only (fundamentalSeq_cofinal), and the least-fixed-point face gives that every fixed point of α ↦ ω^α fires (hfp_from_epsilon_zero). Both directions are now stated, each with its face; deriving hε₀ is open from the 2-adic structure. The box title and both tables name the monotone map sending the tower's stages to c₀ beside hε₀. 'not the states reached' is 'not the set of reachable states'; Birkhoff cited as Birkhoff 1937 via Chakir-Pouzet, arXiv:0812.2300, Thm 2.1(c). Companion reviewed, unchanged. v1.28: ADVERSARY GATE ROUND 3 (bedrock, D1): the OQ-A1a second paragraph said 'given hε₀, nothing below ε₀ fires', dropping snap_unconditional's monotonicity and tower-stage hypotheses; it now names the monotone map sending the tower's stages to c₀. v1.27: DECISION BATCH REMEDIATION AFTER GATE ROUND 2, SECOND PASS (Tim ruling, 2026-09-15): the placement of the step in the ordinal tower pointed at OQ-E2, the wrong object; the box title, both tables, the OQ-A1a second paragraph and the minimal-element note now name the alignment hypothesis hε₀, whose derivation is open as the Classical.choice inversion conjecture (ZeroParadox/Ordinal/Incompleteness.lean). v1.26: DECISION BATCH REMEDIATION AFTER GATE ROUND 2 (Tim rulings, 2026-09-15): OQ-A1a restated in two parts, no algebraic reason to restrict increments to join-irreducibles (exists_supIrred_decomposition, well-founded carriers) and the placement in the ordinal tower conditional on OQ-E2 (snap_unconditional, both faces of eps0); 'answered by crossing charts' removed from the box title and both tables; minimal-element note states both faces and that the placement is the alignment open as OQ-E2; finite-step sentence scoped short of a top; Reading given an antecedent; page 1 scopes every claim to claims proved here. v1.25: DECISION BATCH REMEDIATION ROUND 2 (Tim rulings, 2026-09-15): the OQ-A1a paragraph cites snap_unconditional (hypothesis hε₀) instead of snap_exactly_at_epsilon_zero; the minimal-element note charts the existence claim: AX-B1 in the lattice chart, epsilon-0 the least fixed point in the ordinal chart (epsilon0_min_eq_max), with the lattice step placed there given occurrence (snap_unconditional). v1.24: DECISION BATCH REMEDIATION (Tim, 2026-09-15): OQ-A1's status is now 'OQ-A1b CLOSED by A1-A4; OQ-A1a answered by crossing charts (snap_exactly_at_epsilon_zero)' in the box title and both table cells; the box status carries Tim's confirmed text (the natural numbers under max have a first distinct state above every n, so first steps do not give the ascending chain condition; the ordinal tower crossed into the lattice by a monotone map steps at epsilon-0 and at no ordinal below it, occurrence a hypothesis there; on an arbitrary lattice nothing restricts which increment is taken) and 'No step is guaranteed' replaces the T5 iteration clause. The closure 'a consequence of ZP's binary existence commitment and the occurrence commitment', the minimum-step-above-each-state claim and the MachinePhase parenthetical are removed. v1.23: OCCURRENCE COMMITMENT DEFINED, T5 RESTATED, T-SNAP RESIDUE (Tim decision batch, 2026-09-14): OQ-A1 is closed within ZP given AX-B1 and the occurrence commitment (ZP-E T5, restated). OQ-A1b said AX-B1 bounds ascending chains via T5; measured false (the natural numbers have a first step above every element and an unbounded strict chain), so OQ-A1b is answered yes and the note no longer says the chains are bounded. v1.22: CC-1 STATUS SYNC (2026-09-13): "CC-1 derived" collapsed two readings; cc1_derived proves the conditional (a Quine-atom start is a ⊥ start) and t_exec_iff the converse, so the starting-point choice is restated through the Quine-atom role, not forced. CC-1 box, status line and table cells now say Conditional Claim at ZP-A scope. v1.21: OQ-A1b minimal-element note corrected (bedrock, cross-document attribution) — struck "a metric result, established by ZP-B's 2-adic structure"; ZP-B proves no minimality and cannot (2-adic norms accumulate at 0; its eps0=2ᵏ is a chosen cutoff). Axiom-mistaken-for-result: the preceding paragraph already attributes this to AX-B1, so the two contradicted each other. Now rests on AX-B1 (per CLAIMS.md); ZP-B's actual results at 0 stated (clopen T3, irreversible C3); dense counterexample points at ZP-F (axb1_fails_in_ordered_field).v1.20: R-AFA false premise corrected (bedrock, same class as ZP-E v3.24) — struck BOTH squeeze legs (well-founded⟹external-interpreter; bounded ∈-rank vs unbounded surprisal — orthogonal, ∅ has rank 0 yet v₂(0)=∞); Foundation-incompatibility now rests on ⊥={⊥} self-membership (no_quine_atom); R3/L-INF demoted to corroboration; forcing machine-checked (QuineHost); falsifier narrowed; now consistent with the corrected ZP-E R-AFA.v1.19: rendered Lean citations synced to post-reorg files/namespaces (SSOT-driven).C8 dual-date templating (Initial/Current meta line; hardcoded month removed).v1.17: C8 dual-date — subtitle templated via version_line (First released / This version); hardcoded month removed. v1.16: FMC uniformity (Step 4b) — residual AFA-necessity assertions softened to "argued" (metatheoretic declaration, R-AFA cross-framework note, CC-2 validation-table cell); comp v1.10 same. v1.15: FMC precision (sweep Step 4, against fmc.md) — CC-2 box "structurally required"/"ruled out"/"incompatible" softened to "argued"; named falsifier added; status line marked argued, not a derivation. v1.14: CC-2 label → Forced Metatheoretic Commitment — formal:8c003b4e comp:8e00c888 |
| ZP-B p-Adic Topology | v1.18 | ZP-B_pAdic_Topology.pdf | v1.16 | N/— | v1.18 / comp v1.16: GATE ROUND 4 DEFAULT (2026-09-15): AX-B1's lead clause drops ', the bottom included' (it presupposes the bottom has something above it, false on a one-point carrier); the rest of the sentence is unchanged. v1.17 / comp v1.15: DECISION BATCH REMEDIATION AFTER GATE ROUND 3 (Tim defaults, 2026-09-15): AX-B1's lead clause aligned with its gloss: AX-B1 holds at every state with anything above it, the bottom included; a state with nothing above it owes no step. The ZP-B box adds '(In this two-state chart, 1 is the top.)'. v1.16 / comp v1.14: DECISION BATCH REMEDIATION AFTER GATE ROUND 2 (Tim rulings, 2026-09-15): AX-B1 holds at the bottom and at every iterative bottom, every state with anything above it having a first distinct state above it; a state with nothing above it owes no step. Companion: the field derivation cites AX-B1's two states (T0 Step 1). v1.15 / comp v1.13: DECISION BATCH REMEDIATION (Tim, 2026-09-15): AX-B1's scope stated in its box and in the companion: AX-B1 holds at every iterative bottom (every state, as the base the next step starts from, has a first distinct state above it, with nothing strictly between). Still the one substantive commitment. v1.14: D5 + R1 self-contradiction resolved (Tim, 2026-07-29) - eps0 is UNITLESS. The box asserted it was "a dimensionful quantity" (the v1.12 wording below), then disclaimed any physical-unit magnitude claim, then labelled the status "universe-contingent parameter" - three sentences about three different objects. eps0 is a POSITION (Ordinal.epsilon 0, Veblen (1,0)) and a position carries no units. What is contingent is D5's real-valued threshold 2^k on the CHOSEN exponent k - a normalisation, not a measurement. R1's "determined by physical constants" corrected in the same change; Valuation/Padic.lean likewise. **This retracts the v1.12 dimensionful/Buckingham-pi framing recorded below.**v1.13: D5 exponent/valuation conflation corrected - "eps0 = 2^k where k is the maximum valuation accessible" contradicts the document's own ball convention B(0, 2^-n), since a MAXIMUM valuation gives a MINIMUM scale: k = -v. No semantic change; Valuation/Padic.lean carried the same conflation and is corrected with it.v1.12: D5 Planck-scale reference struck - the last live site in the corpus (Foreword cut it v1.8, both companions v1.3, ZP-B never did). An adversary review called it a crank signal in 2026-05. Replaced with the structural reason the value is contingent: eps0 is DIMENSIONFUL, so its value depends on a unit choice and cannot be instantiation-independent.comp v1.12: PRECISION FIX (release-prep) — grounded the companion's clopen/distance prose in the formal ZP-B claims (C3 no-continuous-path, T5 total disconnectedness): zero is a LIMIT of nonzero states (not isolated; the v1.4/v1.7 correction), v₂(0) is an infinite VALUATION not a distance (2-adic distances stay finite), placing zero in a clopen class distinct from every nonzero state; the only crossing is the discrete-jump Snap. General-reader register kept.C8 dual-date templating (Initial/Current meta line; hardcoded month removed).v1.10: rendered self-version tags removed from C2/C3 corollary titles + "null state" → ⊥ in AX-B1 note (C1 sweep) — formal:df43d2f5 comp:79b374cc |
| ZP-F The Counterexamples | v1.8 | ZP-F_The_Counterexamples.pdf | v1.14 | N/— | v1.8: DECISION BATCH REMEDIATION AFTER GATE ROUND 2 (Tim rulings, 2026-09-15): the opening's occurrence clause and its non-sequitur 'so' removed; the ordered-field setting says only what holds there. v1.7: DECISION BATCH REMEDIATION ROUND 2 (Tim rulings, 2026-09-15): the opening's 'occurrence is a commitment of the framework rather than one of its theorems' now says the Snap occurring follows from the occurrence commitment (instantiation occurs) together with DA-1 (closed given DP-2). Companion reviewed, unchanged. v1.6: FORCING OVERCLAIM RETRACTED. The document described the snap as a forced transition with no occurrence hedge anywhere. T-SNAP fixes the transition's SHAPE; tsnap_holds_but_nothing_moves proves it holds in a model where nothing moves, so occurrence is a framework commitment. Prose only.comp v1.14: FORCING OVERCLAIM RETRACTED (bedrock, cross-document attribution). The key-result table asserted "The snap is a theorem - the valuation gap forces it"; Valuation/Padic.lean calls that ground FALSE by name in the same push - the valuation gap yields no first step, and no metric result could. The first step is AX-B1, a commitment. Section IV heading and its body sentence likewise moved from "forces the snap" to "makes the snap possible" / "becomes available".C8 dual-date templating (Initial/Current meta line; hardcoded month removed).v1.4: vocab fix — "null state" → "⊥" in preamble; palette rebuild — formal:bfed0e0f comp:d6bdb1f7 |
| ZP-C Information Theory | v1.24 | ZP-C_Information_Theory.pdf | v2.9 | N/— | v1.24: DECISION BATCH REMEDIATION (Tim, 2026-09-15): L-INF's note and T-BUF Step 2 and status called DA-1 a design principle; each now matches Remark R5, a Derived Proposition closed given DP-2. Companion reviewed, unchanged. v1.23 / comp v2.9: OCCURRENCE COMMITMENT DEFINED, T5 RESTATED, T-SNAP RESIDUE (Tim decision batch, 2026-09-14): the occurrence commitment is instantiation occurs, and that the Snap occurs follows from it together with DA-1 (closed given DP-2); Remark R5, the Open Items AX-1 row and validation rows R1 and R5 carry that. R5 no longer says DA-1 "must" tie instantiation to execution, and TQ-IH no longer says the AX-1 derivability pathway is "now open". Companion: the key result box put DA-1 on the shape side of the chain; the shape chain now runs L-RUN to T-SNAP and the occurrence side follows it. v1.22 / comp v2.8: AX-1 RETIRED, CONTENT SPLIT (Tim, 2026-09-14): AX-1 is retired; its content was split in two, the shape proved as Theorem T-SNAP and occurrence stated separately as the occurrence commitment. Remark R5's status label and the Open Items AX-1 row credited T-SNAP with all of AX-1 and now say T-SNAP proves the shape and occurrence is stated separately; the companion key result box said the Binary Snap is "no longer assumed" and now says its shape is derived and its occurrence a commitment. v1.21: self-containment note names the ZP-A cross-layer dependency (T-BUF invokes ZP-A D2, the state-transition definition; Lean Order/Lattice.lean).v1.20: rendered Lean citations synced to post-reorg files/namespaces (SSOT-driven).C8 dual-date templating (Initial/Current meta line; hardcoded month removed).v1.18: rendered self-version refs removed — P₀ note ("Version 1.4 updates this") + Open Items row (C1 sweep) — formal:b206f0a9 comp:e98d8d80 |
| ZP-D State Layer | v1.15 | ZP-D_State_Layer.pdf | v1.13 | N/— | v1.15 / comp v1.13: rendered Lean citations synced to post-reorg files/namespaces (SSOT-driven).v1.14: R-PA prior-art note — T is the standard p-adic ball-indicator ONB (van der Put basis) / Kozyrev p-adic wavelet basis (Kozyrev 2002), with p-adic-QM context; paired with the CLAIMS Convergence row. C8 dual-date templating (Initial/Current meta line; hardcoded month removed).v1.12: rendered self-version refs removed — DP-1 title tag + T2 "R3 (v1.6)" (C1 sweep) — formal:923468a5 comp:32064117 |
| ZP-E Bridge Document | v3.39 | ZP-E_Bridge_Document.pdf | v1.18 | N/— | v3.39: DA-1/KLEENE CLASS, GATE ROUND 4 SECOND PASS (Tim ruling, 2026-09-15): Remark R-DA1 now says DA-1 is closed given DP-2 (da1_minimal_path), with ZP-A CC-2 and R3 motivating it and ZP-C L-INF as independent corroboration. Companion reviewed, unchanged. v3.38: DA-1/KLEENE CLASS, GATE ROUND 4 (Tim rulings, 2026-09-15): T5 Selection's 'every landing from epsilon-0 up fires (epsilon0_min_eq_max)' is now 'every fixed point of alpha -> omega^alpha fires (hfp_from_epsilon_zero, epsilon0_min_eq_max)'; the Path 3 conclusion reads 'Given DP-2, instantiation and execution are the same act (DA-1)'. Companion reviewed, unchanged. v3.37: DECISION BATCH REMEDIATION AFTER GATE ROUND 3, SECOND PASS (Tim ruling, 2026-09-15): the DA-1 Status block's 'Kleene fixed-point is the in-scope formal counterpart' of AIT now says the Kleene structure is its in-scope counterpart, carried as a KleeneStructure requirement (botCode_is_quine), not a proof of the AIT claim. v3.36 / comp v1.18: DECISION BATCH REMEDIATION AFTER GATE ROUND 3 (Tim rulings, 2026-09-15): T5 Selection credits each face of ε₀ with its own direction (supremum face: nothing below ε₀ fires, fundamentalSeq_cofinal; least-fixed-point face: every fixed point of α ↦ ω^α fires, hfp_from_epsilon_zero), replacing 'Both faces of ε₀ carry this'; deriving hε₀ is open from the 2-adic structure; the Open Items OQ-A1 cell names the monotone map and tower stages beside hε₀. DA-1 PATH 3 (pre-existing bedrock, editorial round 3 B1): 'Paths 1 and 3 are formally closed / IN LEAN SCOPE', 'DA-1 is grounded in Paths 1 and 3', 'closed in Lean via ZP-K' and the companion's 'the same structural fact' / 'two Lean-verified structural paths' now carry the CLAIMS.md DA-1 row (Path 1 witnessed by da1_closed_concrete, nothing computational; Path 3's witness a KleeneStructure requirement, not a second independent proof; the one-fact reading is the framework's; DA-1 closed given DP-2). v3.35: ADVERSARY GATE ROUND 3 (bedrock, D1): the T5 traceability row said 'given the alignment hypothesis hε₀, nothing below ε₀ fires', dropping snap_unconditional's monotonicity and tower-stage hypotheses; it now names the monotone map sending the tower's stages to c₀. v3.34: DECISION BATCH REMEDIATION AFTER GATE ROUND 2, SECOND PASS (Tim ruling, 2026-09-15): the hε₀ pointer named OQ-E2, which here is the cardinality-semilattice correspondence; the T5 Selection text, the traceability row and the Open Items OQ-A1 cell now name the alignment hypothesis and the Classical.choice inversion conjecture (ZeroParadox/Ordinal/Incompleteness.lean). v3.33 / comp v1.17: DECISION BATCH REMEDIATION AFTER GATE ROUND 2 (Tim rulings, 2026-09-15): T5 selection restated to what snap_unconditional proves (placement at eps0 is the hypothesis h-eps0, the alignment open as OQ-E2; both faces, fundamentalSeq_cofinal and epsilon0_min_eq_max); no rung is ⊥, cited for every rung (Ordinal.epsilon_pos); finite-step sentence scoped short of a top; Reading antecedent; T5 gloss 'Forcing names the shape of each step taken'; OQ-A1 cell and traceability row synced. Companion: the chain diagram puts DA-1 on the occurrence row. v3.32: DECISION BATCH REMEDIATION ROUND 2 (Tim rulings, 2026-09-15): the OQ-E1 row names instantiation occurring as the occurrence commitment, not 'a framework commitment'. v3.31 / comp v1.16: DECISION BATCH REMEDIATION (Tim, 2026-09-15): the T5 box's selection half (the minimum viable step alpha_n = eps(S_n), a Conditional Claim given AX-B1's discreteness at every state) is replaced by Tim's ordinal-chart text (succession_succ, SnapSuccession section I, snap_exactly_at_epsilon_zero; on an arbitrary lattice no step is selected), and its iteration half by 'No step is guaranteed' with the epsilon0_min_eq_max reading. R-DA1 points at the box; the Open Items OQ-A1 row, the T5 traceability row and the validation row are synced ('DA-1 insert section VI'). The branching-tree implication and the companion's legal-case box say the Snap occurring follows from the occurrence commitment together with DA-1 (closed given DP-2); the companion's chain caption is kept with its diagram. v3.30 / comp v1.15: OCCURRENCE COMMITMENT DEFINED, T5 RESTATED, T-SNAP RESIDUE (Tim decision batch, 2026-09-14): the occurrence commitment is instantiation occurs; DA-1 (closed given DP-2) says a configuration at P0 is executing; together they give that the Snap occurs, and T-SNAP fixes its shape. The canonical AX-1 sentence is updated at the abstract, Conclusion, R-DA1, Open Items and the AX-1 traceability and validation rows; the abstract and the T-SNAP traceability rows no longer route the shape through DA-1. T5 (Iterative Forcing Theorem), recovered from ZP-E v1.4, is restated in section VI: selection a Conditional Claim given AX-B1's discreteness at every state, iteration the occurrence commitment applied at each step; OQ-A1 is closed given AX-B1 and the occurrence commitment. The false "All other ZP-E theorems (T1-T7, T2-C) - Unaffected" row now says T1-T4, T6, T7 were stated in ZP-E v1.4 and are not restated here. T-SNAP's name gloss: "Causality" refers to the shape of the step, not to its occurrence. Open Items: "with no Lean kernel axioms". Companion: canonical AX-1 sentence; the occurrence commitment glossed as instantiation occurs; the Four Descriptions paragraph keeps the discrete-state chart and the ordinal chart apart (the ordinal eps0 least fixed point AND tower supremum; no embedding claimed). v3.29 / comp v1.14: T-SNAP PREMISES POINT AT LEAN (2026-09-14): t_snap_given takes two premises, CC-1 (S0 = ⊥) and occurrence at the first step (S1 ≠ S0), as hypotheses over any join-semilattice with bottom; t_snap_derived is its MachinePhase instance at a sequence chosen to move, and MachinePhase does not discharge occurrence. Premises of T-SNAP names it, the registers and endnote point there, and the CC-1 arc round-4 residue is cleared. AX-1 RETIRED, CONTENT SPLIT (Tim, 2026-09-14): AX-1 is retired; its content was split in two, the shape proved as Theorem T-SNAP and occurrence stated separately as the occurrence commitment. Companion four-framework diagram (ZPE-COMP-FOURFRAME-OVERFLOW): the internal caption string that ran off the page and duplicated the caption below is dropped, and the boxes are widened so "ZP-C: Info Theory" fits. v3.28 / comp v1.13: CC-1 STATUS SYNC (2026-09-13): "CC-1 derived" collapsed two readings; cc1_derived proves the conditional (a Quine-atom start is a ⊥ start) and t_exec_iff the converse, so the starting-point choice is restated through the Quine-atom role, not forced. CC-2 is the Forced Metatheoretic Commitment whose Quine-atom role T-EXEC shows only ⊥ fills, not 'a structural consequence'; DA-1 Path 1's ⊥ ∈ ⊥ / ⊥ = {⊥} direction corrected. v3.27: C-DA2 RETRACTION COMPLETED (bedrock, adversary). v3.26 downgraded the Corollary and rewrote its section but never touched the Validation Status table's STATUS cell, which still shipped "Valid - Derived. Follows directly from DA-2 and ZP-B C3" four pages after the section saying the opposite, on the one page a reader opens to check status. A half-applied fix to a self-consistent error manufactures a self-contradiction. Verified closed at the rendered bytes, not at the source.v3.26: NOVELTY OVERCLAIM RETRACTED (bedrock) - C-DA2 downgraded from Corollary to CONDITIONAL CLAIM. Its published proof was circular: the load-bearing step, that the bottom of I_{n+1} lies in a distinct topological space, IS the conclusion and is not obtained from C3, which quantifies only over paths within one space. Handle C-DA2 unchanged per R-NAMING; status changed. snap_arc_z2_loop and tower_image_loops_to_seed measure against it - the 2-adic arc returns to the SAME 0. v3.25 / comp v1.12: FORCING OVERCLAIM RETRACTED. The document asserted that T-SNAP establishes the snap OCCURS; it does not. T-SNAP fixes the transition's shape, and Order/Snap.lean's NO-GO gauge tsnap_holds_but_nothing_moves proves T-SNAP holds in a model where nothing moves. Occurrence is a framework commitment. Prose only. Four ZP-E sites (the branching-tree implication, the OQ-E1 row, and two claim-table validity grounds); OQ-E1 is now "closed given the occurrence commitment", matching CLAIMS.md and the DA-1 row's existing "closed given DP-2" form. The companion described "the forced first transition in any join-semilattice", which the gauge itself refutes (its MachinePhase IS a join-semilattice in which nothing moves); its § on DA-1 also reworded to drop a banned vocabulary term that had been blocking its rebuild.v3.24: R-AFA false premise corrected (bedrock) — struck the invalid well-founded⟹finite-tree step (ω is well-founded and infinite); Foundation-incompatibility now rests on the self-membership of ⊥ = {⊥} (no_quine_atom, choice-free); forcing + realizability stated machine-checked (QuineHost — quineHost_not_wellFounded, oneAtom_not_wellFounded, afaStructure_isQuineHost); CC-2 = Forced Metatheoretic Commitment (not Conditional Claim); named falsifier narrowed to the requirements-choice; CC-1 "modelling commitment" → "derived via ZP-J", DA-3 link hedged to conjecture, endnote de-changelogged.v3.23: rendered Lean citations synced to post-reorg files/namespaces (SSOT-driven).C8 dual-date templating (Initial/Current meta line; hardcoded month removed).v3.21: FMC precision (sweep Step 4, against fmc.md) — R-AFA "the metatheoretic necessity of AFA is derived" → "argued, not proved (a metatheoretic squeeze, not a derivation)" + named falsifier added; CC-2 status lines split the proved structural fixed point (T-EXEC, axiom-free) from the argued set-membership reading; "establish that Foundation is incompatible" → "make the case that" — formal:c20ee8f2 comp:5a9afde4|
| ZP-G Category Theory | v1.15 | ZP-G_Category_Theory.pdf | v1.9 | N/— | v1.15: rendered Lean citations synced to post-reorg files/namespaces (SSOT-driven).v1.14: R-AX named AX-G2 as the standard strict-initial-object property (Carboni-Lack-Walters 1993) + the AX-G1+AX-G2 = non-trivial-strict-initial note; prior-art positioning paired with the CLAIMS Convergence section. C8 dual-date templating (Initial/Current meta line; hardcoded month removed).v1.12: rendered self-version refs removed — 24 status/provenance tags + "Version 1.0…1.1" narratives + Supersedes (C1 sweep; cross-doc ZP-H v1.0 citations kept) — formal:2af3526b comp:9b61e514 |
| ZP-H Categorical Bridge | v1.23 | ZP-H_Categorical_Bridge.pdf | v1.16 | N/— | v1.23: DECISION BATCH REMEDIATION ROUND 2 (Tim rulings, 2026-09-15): T-H3's 'That the transition is taken remains a framework commitment' now says the Snap occurring follows from the occurrence commitment (instantiation occurs) together with DA-1 (closed given DP-2). comp v1.16: DECISION BATCH REMEDIATION (Tim, 2026-09-15): the T-H3 page scoped 'derived as a theorem (T-SNAP), not assumed as an axiom' and 'a structural fact that survives translation' to the shape, with the canonical AX-1 sentence. v1.22: OCCURRENCE COMMITMENT DEFINED, T5 RESTATED, T-SNAP RESIDUE (Tim decision batch, 2026-09-14): the ZP-E import line, the Open Items AX-1 row, and the IR-ZP, IR-2 and AX-1 validation rows say that the Snap occurs follows from the occurrence commitment (instantiation occurs) together with DA-1 (closed given DP-2); Open Items says "with no Lean kernel axioms"; T-SNAP's readable name is glossed once at T-H3. v1.21: AX-1 RETIRED, CONTENT SPLIT (Tim, 2026-09-14): AX-1 is retired; its content was split in two, the shape proved as Theorem T-SNAP and occurrence stated separately as the occurrence commitment. The ZP-E import line, the Open Items AX-1 row, the IR-ZP and IR-2 validation rows and the AX-1 validation row scope the derivation to the shape. Companion reviewed, unchanged: it says T-SNAP is a derived theorem, not assumed as an axiom, which stays true because T-SNAP is the shape theorem. v1.20 / comp v1.15: CC-1 STATUS SYNC (2026-09-13): "CC-1 derived" collapsed two readings; cc1_derived proves the conditional (a Quine-atom start is a ⊥ start) and t_exec_iff the converse, so the starting-point choice is restated through the Quine-atom role, not forced. v1.19: AX-B1 OVERCLAIM RETRACTED (bedrock). "No novel axioms: AX-B1 decidable" - AX-B1 is the framework's ONE substantive modelling commitment, and `decide` only checks the two states are distinct GIVEN the two-element type. The Foreword retracted this exact wording at v2.12; ZP-H was the surviving twin. v1.18: FORCING OVERCLAIM RETRACTED (Remark R-FORCING). F_B was said to "force an irreversible jump at 0" - Valuation/Padic.lean retracts exactly that ground: the 2-adic topology proves irreversibility and a clopen gap, never a first step. The closing "structurally forced across all four" now scopes to the SHAPE; occurrence stays a framework commitment.C8 dual-date templating (Initial/Current meta line; hardcoded month removed).v1.16: rendered self-version removed — endnote + R-FORCING tag (C1 sweep; 21 cross-doc citations kept per scope) — formal:cece605b comp:c89777ab|
| ZP-H Native Categories Addendum | v1.6 | ZP-H_Native_Categories_Addendum.pdf | N/A | N/— | v1.6: MC-1 PREMISE (claim-review gate round 4 bedrock, Tim confirmed, 2026-09-15): both seam_unique_among_named pointers read 'for the named bottoms, in a lattice with no top'. v1.5: ADVERSARY GATE ROUND 3 (bedrock, D1): the MC-1 section body still called the four bottoms being numerically one object a modeling commitment; it now states the retirement as ill-typed, as page 1 does. v1.4: DECISION BATCH REMEDIATION AFTER GATE ROUND 2 (Tim rulings, 2026-09-15): MC-1 identity retired because object equality across categories does not typecheck and is not invariant under equivalence; what separates the members is proved property by property (seam_unique_among_named, for the named bottoms). v1.3: DECISION BATCH REMEDIATION ROUND 2 (Tim rulings, 2026-09-15): the one-object reading 'remains a modeling commitment, not a theorem' is replaced: the MC-1 identity is retired as ill-typed (neither x = y nor x != y is well-formed across categories) and the members differ by property, each in its own category (seam_unique_among_named). C8 dual-date templating (Initial/Current meta line; hardcoded month removed).v1.2: rendered Lean-file citations synced to post-reorg basenames (ZPH_* -> *, ZPH_MC1 -> MC1Bridge).v1.0: Initial release — snap floor realized in native Mathlib categories: fB_functor → TopCat (⊥ = inverse limit ⋂ B(0,2⁻ⁿ) = {0}), fD_functor → ModuleCat ℂ (⊥ = StateSpace 0 initial; isometric embeddings), fC_functor → KleisliCat PMF (⊥ = Fin 0 initial; fC_no_return = AX-G2 as theorem); mc1_correspondence capstone. Correspondence half of MC-1 formal; identity half retired as ill-typed (members provably distinct). Lean: ZPH_TopFunctor/ZPH_HilbFunctor/ZPH_InfoFunctor/ZPH_MC1.lean; [propext, Classical.choice, Quot.sound] — formal:c9da80f9 |
| ZP-I Inside Zero | v1.26 | ZP-I_Inside_Zero.pdf | v1.32 | N/— | v1.26: DA-1/KLEENE CLASS, GATE ROUND 4 (Tim ruling, 2026-09-15): the ninth site 'DA-1 is closed GIVEN DP-2 by ZP-K via Kleene's second recursion theorem' now reads closed given DP-2 (da1_minimal_path), ZP-K witnessing its Path 1 (da1_closed_concrete). Companion reviewed, unchanged. comp v1.32: DA-1 CREDIT (Tim rulings, 2026-09-15), companion sync with ZP-I v1.25: the step 3 row, the da1_computational status entry and the Summary credited DA-1 at the successor bottom to the computational fixed-point argument; da1_computational proves only that the bottom of a KleeneStructure lattice is its Quine atom, and DA-1 is closed given DP-2 (ZP-E), with ZP-K witnessing Path 1 and carrying Path 3 as a KleeneStructure requirement. v1.25: DA-1 CREDIT (Tim ruling, 2026-09-15): eight sites said ZP-K formally closes DA-1 via Kleene; each now says DA-1 is closed given DP-2 (ZP-E), with ZP-K witnessing Path 1 and carrying Path 3 as a KleeneStructure requirement. Companion reviewed, unchanged. v1.24: OCCURRENCE COMMITMENT DEFINED, T5 RESTATED, T-SNAP RESIDUE (Tim decision batch, 2026-09-14): the Open Items AX-1 row carries the canonical AX-1 sentence; page 1 says T-SNAP fixes the shape of the first transition and not that it is taken; Remark R-II.2 names t_iz_limit_is_new_null's declaring file (ZeroParadox/Valuation/SemilatticeInstance.lean) beside its index (ZeroParadox/Order/SnapCannotBe.lean). v1.23: AX-1 RETIRED, CONTENT SPLIT (Tim, 2026-09-14): AX-1 is retired; its content was split in two, the shape proved as Theorem T-SNAP and occurrence stated separately as the occurrence commitment. The Open Items AX-1 row ("AX-1 retired. T-SNAP is derived.", CLOSED) credited T-SNAP with all of AX-1; it is now RETIRED with the split stated, and the Remaining axioms row no longer calls AX-B1, AX-G1, AX-G2 "the three foundational commitments". Companion reviewed, unchanged: it already says T-SNAP's shape is derived and its occurrence committed to. v1.22 / comp v1.31: THE CLAIM IS DELETED, NOT REDRAFTED (adversary FAIL-BEDROCK, round 2). Fourth round on the same sentences and the counts rose, 1 -> 2 -> 1 -> 3, so R-REVALIDATE governs: measure the claim, then restate to the measurement or DELETE, never reword again. The measurement is a counter-model. R1's Lean form t_snap_irreversible is universal over every ZPSemilattice, so it holds in the one-point carrier, where HasNoTop is FALSE - the single element is its own top and there is no room to ascend. A property satisfied where there is no room does not supply the room, so "R1 leaves the chain somewhere to go" is false rather than loose, and it is deleted from both documents instead of rephrased a fifth time. Companion also: the step-1 SOURCE cell credited t_iz_cauchy to R1, when it binds the chain and the norm bound and no lattice at all; and the Lean status box gave t_iz_r1_t3_geometric_bound its NEIGHBOUR's binders while dropping the nonvanishing hypothesis the named lemma requires - a status box stating a theorem that is not the theorem. Formal also: the unscoped universal at three sites now says chains that strictly ascend at every step, matching the strict-ascent hypothesis the section-Ia chain consumes; "both paths begin from the no-top property" and "structural consequence of ZP-A through ZP-E" deleted; the banned "arrow of time is monotonicity" removed, invisible to every source grep because it spans two adjacent string literals. Rounds 3-5 then retracted four more, each found by a gate and each verified at the RENDERED bytes: the replacement sentence for the R1 claim, which widened "the one-point carrier" to "carriers that have a top" and was refuted by MachinePhase; "pure ZP-A lattice conditions" at six sites, where exactly one of three hypotheses is a lattice condition; a completeness non-sequitur whose ROOT CAUSE was in the Lean and was fixed there first; ZP-B T2 credited for a metric conclusion when it is "every ball is clopen", found independently by both gates; the round-4 fix for that dropping IsStrictStateSequence, since T3 alone is monotone; and an axiom-provenance claim that the Classical.choice was not inherited from the p-adics, measured false at the TYPE. The BEDROCK cap of five rounds is exhausted.v1.21 / comp v1.30: THE NAME FIX WAS NOT THE ATTRIBUTION FIX (claim-review FAIL-BEDROCK, round 1). v1.20 resolved R1's name collision by replacing "R1 + T3" with "no-top + T3" at thirteen sites, which corrected the NAME and left the FALSE ATTRIBUTION standing in a form that looked repaired. Two orthogonal defects treated as one. The gate extracted every signature in SemilatticeInstance.lean, eighteen declarations, and found HasNoTop in ZERO binders: t_iz_r1_t3_geometric_bound binds S, hS, h_strict; h_strict_from_r1_t3 binds S, depths, IsDepthChain, IsStrictStateSequence. T3 IS legitimately consumed, through IsStateSequence nested inside IsStrictStateSequence; the no-top property is consumed nowhere in the chain. Every attribution now names what the theorem actually binds. The worst site was a KEY-RESULT BOX refuting itself in three bullets - bullet 1 "Lean-derived from no-top + T3", bullet 3 "Only the second appears in h_strict_from_r1_t3's binders", with bullet 3 still saying "R1" for the order property - plus two companion LEAN STATUS BOXES, which are the surfaces a reader treats as the verified summary. Root cause fixed FIRST in the Lean: Order/Lattice.lean's HasNoTop docstring read "Algebraic expression of unbounded ascent: the framework never terminates", the occurrence claim, four lines below the section warning saying the opposite, and that docstring is what every hover and every source-drafting agent reads first. Section Ia had also listed HasNoTop as an INPUT to a derivation it is not in. v1.20 / comp v1.29: NAME COLLISION RESOLVED, AND TWO OVERCLAIMS THE v1.19 FIX ITSELF INTRODUCED (both gates FAIL-BEDROCK, round 1 of a reset arc). R1 named TWO DIFFERENT PROPOSITIONS: ZP-A's Remark R1 is the NO-SUBTRACTION restriction (build_zpa.py, exported as "no subtraction / additive ontology"; t_snap_irreversible cites it that way), while ZeroParadox/Order/Lattice.lean headed its own section "R1 - No Top Element" and defined HasNoTop under it - and ZP-I used BOTH meanings inside one PDF, its section I against its own "ZP-A R1 - No Subtraction" heading. Tim chose to remove the ambiguity rather than annotate it: the ORDER property is named HasNoTop and cited to Order/Lattice, "ZP-A R1" is reserved for no-subtraction, and nothing is renamed because HasNoTop was already the identifier. This is very likely why the enabling-credited-as-forcing family recurred five times - "R1 forces the convergence" is nearly defensible under no-subtraction and flatly false under no-top, so a reviewer checking whether R1 is cited correctly clears it either way, of different R1s. RETRACTED with it, and introduced BY the v1.19 fix: that no-top puts the limit OUTSIDE L, hence a new bottom, hence the succession. A compiled counterexample refutes it - the ordinals under max are a ZP-A semilattice, satisfy HasNoTop, and the chain n to n has least upper bound omega INSIDE them. Whether a limit escapes its carrier is a property of the particular lattice; the framework's own InfinitudeFloor declares floor and member in the SAME type, and tower_height_floor_reconciliation keeps both closures in their own carriers, held apart by the antitone map. AND the v1.19 sweep was HALF-APPLIED: it corrected seventeen sites by grepping a VOCABULARY LIST and missed "as Driver", "cannot stabilise", "forced by ZP-A R1" and "Follows from no-top property", including a subsection heading, a proof-obligation table cell, and a companion diagram arrow label sitting inside a Drawing where no prose checker can see it. Sweep by the SUBJECT, which is stable, not the predicate, which is an open set. Section I's "the chain cannot stabilise" was not imprecise but FALSE, and the document shipped its own counterexample five pages later. Backed by a NO-GO gauge at SemilatticeInstance.lean section Ib, now in two parts: the no-top half is ℕ-specific, and a second generic example proves the stalling half holds at ANY element of ANY ZPSemilattice, so availability fails to force occurrence in every lattice where the question can be posed. v1.19 / comp v1.28: R1 ATTRIBUTION CORRECTED (DC-32 - enabling credited as forcing). Both surfaces said ZP-A R1 (no top element) FORCES the valuation to infinity and called it "the engine of T-IZ" - ten sites in the formal document and seven in the companion, including a companion SECTION TITLE and a remember-box, which is what a general reader takes away. Measured at the signature: h_strict_from_r1_t3 binds S, depths, IsDepthChain and IsStrictStateSequence, and HasNoTop appears nowhere in it. No-top says every element has a strictly greater one, so no chain HAS to stop and the limit falls outside L - RETRACTED at v1.20, refuted by a compiled counterexample; that a particular chain keeps stepping is IsStrictStateSequence. Possibility and occurrence are different claims and only the second is a hypothesis of T-IZ. One companion site listed three conditions as together forcing strict depth growth and omitted the one that does it. The companion already carried an "Occurrence fence" for T-SNAP two paragraphs from an uncorrected R1 site - the same distinction applied to one axiom and not the adjacent one. Backed by a NO-GO gauge added at SemilatticeInstance.lean section Ib, mutation-verified: N has no top, the constant chain is a state sequence in that same lattice which never moves, and dropping the negation gives a type error while restoring gives exit 0. FIFTH occurrence of this claim shape in two months; the prior mechanical response added CARRIER vocabulary to check_pov, which scans these scripts and does not fire, because it enumerates carrier NOUNS (totally disconnected, ultrametric) and "no top" is an order property. NOT a retraction of R1 - deleting it from the account is the opposite error (DC-30) and the accurate form names both roles. Also corrected: the null balance 0 + x + (-x) = 0 is relabelled a READING at both sites, since a ZPSemilattice carries join and bottom with no additive inverse, so the identity is not statable in the structure T-IZ is about, and no declaration derives it. v1.18 / comp v1.27: JOIN-IDENTITY OVERCLAIM RETRACTED (bedrock). Four rounds corrected the NOVELTY conjunct of one sentence while the OTHER conjunct in the same sentence went unmeasured: both surfaces asserted "The Cauchy limit 0 in Q2 satisfies the join-identity condition" as a proved step, cited to t_iz_limit_is_new_null, where that property is the theorem's HYPOTHESIS. Measured at the signature - t_iz_complete takes S : N -> Q2 and, separately, a terminal in an arbitrary semilattice with h_role handed in, and never relates them, so it is a conjunction of independent results rather than a chain. Not merely unproved: Q2 carries no ZPSemilattice join, so the condition is not statable of the limit (ScaleBridge.lean). CLAIMS.md's T-IZ row stated this correctly throughout and was never propagated, which is why rewording never reached it. R-II.2's "the chain from Step 1 to Step 6 is complete" was false as written. Also v1.18: the header banner moved ABOVE the changelog - check_hashes._header_version truncates at the first changelog line, so this script returned None and its fingerprint was silently unverified on every run; and a rendered "SemilatticeInstance.lean v1.1" corrected to the full path, as no .lean file carries a version. SWEEP COMPLETED after both gates returned FAIL-BEDROCK on the half-applied first pass, which corrected six sites and left the claim standing at sixteen - the document then asserted the occupancy on page 1 and called it not-statable on page 5. Root cause fixed FIRST in the Lean: SemilatticeInstance.lean:261 still carried the retracted sentence verbatim, and R-COREOBJ sends every agent to the Lean first, so every round drafted its correction from a source asserting what it was retracting. Framing now has THREE tiers rather than two: convergence PROVED, role-recognition PROVED as an implication, occupancy a COMMITMENT, novelty a further COMMITMENT. comp v1.26: NOVELTY OVERCLAIM RETRACTED - THE comp v1.25 SWEEP WAS HALF-APPLIED (bedrock). comp v1.25 retitled the "bottom-prime is born" row and left three sites standing in the same document; a half-applied fix to a self-consistent claim manufactures a self-contradiction, which is the ZP-E v3.27 shape one document over. Step-table row 2 asserted "The limit is bottom-prime" citing t_iz_limit_is_new_null as warrant, while row 4 of the SAME table already read "(the role, not the novelty)"; the t_iz_limit_is_new_null gloss in SnapCannotBe.lean forbids that citation verbatim, because the theorem runs ROLE implies identity-with-the-bottom-already-present, one direction, inside one lattice - it identifies an occupant and constructs nothing. The "closed system" note ran the derivation chain to bottom-prime where it reaches the bottom ROLE, and the traceability block listed bottom-prime-identification among what t_iz_complete proves "All formal". Novelty is C-DA2, a commitment; snap_arc_z2_loop has the 2-adic arc returning to the SAME 0. Third occurrence in ZP-I. v1.16 / comp v1.25: NOVELTY OVERCLAIM RETRACTED (bedrock). The successor bottom was rendered as a DERIVED result across the theorem box, Step 5, the proof-obligation table, the Open Items status cell, the Null Balance callout and the companion's opening abstract - including a traceability row citing t_iz_limit_is_new_null as the novelty witness, which the t_iz_limit_is_new_null gloss in SnapCannotBe.lean forbids verbatim. That theorem proves the ROLE half only: in a join-semilattice anything acting as the join's identity IS the bottom - role implies identity with the bottom ALREADY THERE, inside one lattice, and it discharges on the one-point lattice where a second bottom is impossible. Novelty is a modelling commitment; snap_arc_z2_loop has the 2-adic arc returning to the SAME 0. Same family as v1.15's occurrence retraction, one commitment further in. comp v1.24: FORCING OVERCLAIM RETRACTED (companion sync with ZP-I v1.15) - "T-SNAP (bottom -> eps0, necessarily)" asserted occurrence; an occurrence fence was added and its scope corrected to cover the whole document.v1.15: FORCING OVERCLAIM RETRACTED. The document asserted that T-SNAP establishes the snap OCCURS; it does not. T-SNAP fixes the transition's shape, and Order/Snap.lean's NO-GO gauge tsnap_holds_but_nothing_moves proves T-SNAP holds in a model where nothing moves. Occurrence is a framework commitment. Prose only. Three sites, all using the word "necessarily" rather than "forced" - which is why two earlier greps did not reach them.v1.14: PURITY CORRECTION (release-prep) — "axiom-free" was wrong for the p-adic convergence spine; t_iz_cauchy / t_iz_valuation_unbounded / t_iz_c3_compatible / t_iz_complete / t_iz_h_bound_from_depth_chain carry Classical.choice (inherited from Mathlib p-adic analysis, not a framework commitment); only Step 6 (t_iz_limit_is_new_null) + t_snap_derived are axiom-free, c_t_iz_null_balance is [propext]; ~15 rendered claims corrected. Verified vs #print axioms.v1.13 / comp v1.23: rendered Lean citations synced to post-reorg files/namespaces (SSOT-driven).C8 dual-date templating (Initial/Current meta line; hardcoded month removed).v1.11: rendered self-version removed — register section headers + T-IZ status cells + endnote (C1 sweep) — formal:e2ed66c0 comp:fdeb337f|
| ZP-J Self-Reference | v2.8 | ZP-J_Self_Reference.pdf | v1.32 | N/— | v2.8 / comp v1.32: CC-1 STATUS SYNC (2026-09-13): "CC-1 derived" collapsed two readings; cc1_derived proves the conditional (a Quine-atom start is a ⊥ start) and t_exec_iff the converse, so the starting-point choice is restated through the Quine-atom role, not forced. Open Items 'ZP-A CC-1 cross-check' RESOLVED; result box titled 'Theorem CC-1, Conditional Form'. v2.7 / comp v1.31: THREE BEDROCK REMEDIATED (ZPJ-ITER, ZPJ-AXFREE, ZPJ-AFA-THM at the ZP-J sites). ⚠ The first draft of this row said CLOSED and editorial round 1 refuted it at the rendered text: "ZP-J's version of this theorem" survived one sentence after the rewrite. Closure is the gates' verdict, not this row's. Also folded in from that round: the companion's CC-2 section ran its implication backwards (bot_self_mem is an INPUT to T-EXEC, not a consequence of the ZP-A axioms; Tim: match the Lean), and round 2 found the fix had reached that section only - every CC-2 site now reads bottom as a ROLE, per Tim ("Bot is a role not a fixed value") and SetTheoryAFA.lean's NO-GO gauge: T-EXEC identifies the occupant of the Quine-atom role, and the literal set fact stays the ZF+AFA commitment; round 3 then found the formal document arguing lattices "must earn their AFA status", measured false (every ZPSemilattice carries AFAStructure, via trivialSelfApp and toAFAStructure), and both documents were swept to Wheel.lean's CC-2 convention (the set identity is never asserted of a carrier; the Lean carries the role as a hypothesis), and "Z_[2] cannot be an instance because it is a ring" is refuted by Scale.lean § V. (1) The cyclic case was described as composing the decoration equation into d(v) = scale^k(d(v)), true only at single-child vertices; cyclic_decoration_eq_bot bounds with collect_val_ge and path_val_chain and never forms scale^[k], read off its elaborated proof term. Both documents and the APG.lean docstrings now state the bound, and the traceability register names what the proof consumes instead of two lemmas it never calls. (2) Six "axiom-free" register cells measured false with lake env lean on 2026-09-13 - five carry Classical.choice and singleton_from_unique_witness carries [propext, Quot.sound]; the document's other axiom-free cells were re-measured and hold. (3) AFA was called a theorem about APGs across both documents (the sites are named in each builder's changelog); Aczel ch. 1 p. 6, opened, states an axiom over every graph. Companion also: the Z_[2] model's choice was credited to Mathlib alone, and q2Val adds its own route through instDecidableEqZ2 := Classical.decEq, so both routes are named; and its opening credited T-EXEC's proof to a valuation structure it does not use. ⚠ ZP-K and Kleene.lean still say "the AFA decoration theorem" - outside this round, left on ZPJ-AFA-THM. v2.6 / comp v1.30: PHANTOM AXIOM REMOVED (bedrock). The rendered DecorationUniverse box listed THREE laws where APG.lean declares TWO, and cited the non-existent collect_ext as the MECHANISM for acyclic_induction_step; the real proof equates the two child image sets by Set.ext and rewrites - ordinary congruence, consuming no class law. Also struck a side condition x != bot from collect_val_ge that the Lean does not carry. Box now copied verbatim from the AFA addendum builder, which transcribes the Lean literally. Companion: AFAStructure's three FIELDS are not three obligations - selfMem is data you supply. comp v1.29: rendered Lean citations synced to post-reorg files/namespaces (SSOT-driven).C8 dual-date templating (Initial/Current meta line; hardcoded month removed).v2.5: rendered Lean citations synced to post-reorg files/namespaces the v2.4 pass missed (ZPJ.lean -> SetTheoryAFA.lean; ZeroParadox.ZPJ.* -> ZeroParadox.* namespace flattened; ZPE.da2_bottom_characterization -> bare, now in Snap.lean).v2.4: rendered Lean-file citations synced to post-reorg basenames (ZPJ_AczelConn.lean -> AczelConn.lean, etc.).v2.2: rendered self-version refs removed (C1 sweep); null glyphs fixed. comp v1.26: FMC precision (sweep Step 4) — Key Results box + T-EXEC body line split the proved structural fixed point (axiom-free) from the argued set-membership reading. comp v1.25: ScaleBridge wired into maintained build → §6 "future work" bridge retired (2-adic argument now formalized), §7 common-ancestor (ValBridge) framing added, §8 adds ℤ₂ as 3rd concrete model (flagged Classical.choice-carrying vs axiom-free T-EXEC). comp v1.24: APG directed-graph diagram (self-loop), arithmetic analogy scoped, depth rephrased to intrinsic descent, self-loop redrawn as dashed curve (Dan 2026-06-15) — formal:d4d99541 comp:c2a20ceb|
| ZP-J AFA Addendum | v1.16 | ZP-J_AFA_Addendum.pdf | N/A | N/— | v1.16: THE v1.15 FIX PUT A REGRESSION INTO THE BOX IT WAS FIXING (editorial FAIL-BEDROCK, round 5). ⭐ v1.15 restored the underscore in the `decoration_unique` binder by ADDING a line reading `(_G : APG V)` and never removing the old `(G : APG V)`, so the rendered box declared **two** APG binders where the theorem has one, and put the survivor BEFORE the `{U}` block instead of after it. Elaborated this round with `lake env lean`: `@ZeroParadox.decoration_unique` takes exactly ONE, `(_G : APG V)`, following `[DecorationUniverse U]`, with `[Fintype V]` ahead of the `{U}` block — the box is transcribed from that signature now rather than edited toward it. ⚠⚠ **Every mechanical gate passed and `check_codebox` reported 0**: that checker resolves IDENTIFIERS, and a duplicated binder is a well-formed identifier used twice, so the defect lives in the SHAPE, which nothing mechanical here reads — the fifth version in this class and the second found only by reading the rendered page. Also: “AFA’s central theorem” survived at TWO FURTHER SITES in this document, the § IV preamble and the scope note after the theorem box. Aczel ch.1 p.6 states AFA as an AXIOM, and this document’s own Remark R-J.A said so on page 8 while those two contradicted it; both now POINT at the Remark rather than restate the claim a third time. ⚠ And v1.15’s “sites five and six” is SCOPED, not closed: it counted within this document, and a four-axis sweep of the RENDERED text on 2026-09-02 found the class live at five further sites across three deposited PDFs, filed as `ZPJ-AFA-THM`. That sweep covered the DELTA domain, 33 files and 6 rendered PDFs; `--full` was not taken, so five is a FLOOR and not a total. v1.15: THE v1.14 FIX OVER-CORRECTED, AND BOTH PROSE GATES CAUGHT IT (editorial FAIL-BEDROCK; adversary concurring, ordinary). ⭐ Editorial's sentence is the finding: *“v1.13 made a removable cost look UNAVOIDABLE, v1.14 makes an unmeasured one look REMOVABLE.”* Same conflation, opposite sign — and v1.14's was the flattering direction. The class-level measurement was TRUE and both gates reproduced it; the SCOPE was wrong, because the box was headed “all results in this document”. Measured this round with `lake env lean`: `decoration_unique` reports `[propext, Classical.choice, Quot.sound]`; `Set.ncard_lt_ncard` and `Set.ncard_pos` are each INDEPENDENTLY tainted; and `Nonempty.some` unfolds to `fun h => Classical.choice h`, so the proof does not inherit choice through packaging — **it calls it**. `Fintype` is not absent either: this PDF prints `[Fintype V]` in the `decoration_unique` box and says the theorem requires it, ONE PAGE before v1.14's line denied it. The box now names three routes, scopes ACCIDENTAL to the CLASS (where `_VScast` exhibits the clean proof), and states the THEOREM's removability as UNMEASURED — `R-REVALIDATE`: accidental is earned by exhibiting a clean proof or not at all. ⚠ The exclusion is MEASURED now rather than asserted, which is the adversary's improvement: `Finset` and `Fintype` each report `[propext, Quot.sound]`, so neither has any choice to supply. Also: the Remark's own TITLE still read “Relationship to Aczel's Theorem” three words above its body's “an axiom, not a theorem”, and the preamble called the unique-decoration content a theorem — **sites five and six** of that claim WITHIN THIS DOCUMENT; the `decoration_unique` box had dropped the underscore from `(_G : APG V)`, erasing the signal that accessibility is never consumed; and `Scale.lean`'s “`val_bot` is consumed nowhere” is true of § I and FALSE three rendered pages later at `val_iterate`, so it is scoped. v1.14: TWO BEDROCK ON PAGE 8, BOTH UNTOUCHED BY EVERY ROUND SINCE v1.9 (adversary round 5, FAIL-BEDROCK 2). **(1) The axiom-footprint box named the wrong source for `Classical.choice`** — “Mathlib Finset and Fintype machinery”. Measured with `lake env lean`: `ValuationStructure`, a BARE TYPECLASS mentioning neither, reports `[propext, Classical.choice, Quot.sound]`, and so does `instAddMonoidWithOneENat`, the instance the numeral `1` in `val_scale` reaches for. `Scale.lean`'s `_VSlit`/`_VScast` pair shows that numeral is THE route and not one of several: respelling it as `((1 : ℕ) : ℕ∞)` and nothing else makes the class report no axioms at all. ⚠⚠ **It mattered beyond a citation slip because the next line read “No ZP-specific axioms beyond”, so crediting a library turned a REMOVABLE cost into an UNAVOIDABLE one** — the precise conflation `AxiomProfile.lean` § 0 warns against: *“PROVENANCE and NECESSITY are independent axes, and the first version of this heading conflated them.”* The corrected text keeps them apart: the tainted INSTANCE is Mathlib's, the SPELLING reaching for it is this framework's, and the dependence is ACCIDENTAL — the same shape `ZeroParadox/Ordinal/SyntacticCollapse.lean` records for Mathlib's ℚ instance, choice-tainted at the INSTANCE level. **(2) Remark R-J.A misstated Aczel.** It read “Aczel's decoration THEOREM … every APG … universe of NON-WELL-FOUNDED sets”. The book is in this repository and was opened: ch.1 p.6, *“The Anti-Foundation Axiom, AFA: Every graph has a unique decoration.”* An AXIOM, over every GRAPH, into the universe of SETS — the book's own worked example decorates with 0, {0} and 3, all well-founded, and the decoration THEOREM is Mostowski's Collapsing Lemma for WELL-FOUNDED graphs. `CLAIMS.md` and `ZeroParadox/Settheory/APG.lean` already carried the correct statement, and this document's own preamble calls it an axiom eight pages earlier. ⭐ **The CLAIM was swept, not the site: FOUR sites**, and the fourth read “Aczel's theorem” with no “decoration”, so a source grep for the longer phrase never reached it — only the RENDERED check did. **Also closes E5-1:** the four Lean-statement lines printing the bottom as a glyph with no global notation now spell it as the Lean does, and `tools/verify/check_codebox.py` — built this session because `R-NOCONV` says a loop that will not settle changes SHAPE — reports **0 where it reported 4**. Fifth consecutive version to touch that class, and the first whose fix is MEASURED rather than believed. v1.13: BEDROCK — THE v1.12 SWEEP MISSED IT BY THREE LINES, IN THE SAME BOX. The APG definition box read `Reach(v) = { w : V | Reachable v w }`. **`Reachable` is NOT LOCATED in `ZeroParadox/Settheory/APG.lean` as of 2026-09-02** — zero occurrences, searched by identifier — and the only resolution located that day, over `ZeroParadox/**/*.lean` and the tracked build scripts, was `SimpleGraph.Reachable`, **which is SYMMETRIC** — and § IV.2's induction terminates ONLY because reachability here runs ONE WAY: from an acyclic v to a child w, v is not reachable back, so `Reach(w)` is a STRICT subset and the cardinality decreases. Under a symmetric relation the two sets are EQUAL, the descent never shrinks, and the proof this document narrates does not terminate — a false definition carrying the main theorem's termination argument, with both witnesses elaborated by the gate. ⚠⚠ **v1.12 changed the `accessible` line DIRECTLY ABOVE IT for this exact reason and left this one**: the half-applied sweep at its smallest possible radius, inside the version whose own subject was half-applied sweeps. Found by taking the rendered BOX as the unit and running it; a source reader sees a diff touching the very line the changelog names and reads it as done. Corrected to `Nonempty (Quiver.Path v w)`, and both polarities swept with `check_paths.py --claim`: the deleted wording returns ZERO sites, the new one exactly two. Also: two inherited COUNTS here replaced by pointers — “four sites” stood over an enumeration of five, and “all fifteen corrections” was v1.11's numeral carried into v1.12 over six of its own. R-ADJACENT: never enumerate in prose what an artifact defines. v1.12: ROUND 4, AND THE SWEEP LANDED ON TWO BOXES OF FOUR AGAIN. Adversary FAIL-BEDROCK (2, both in OTHER ZP-J documents — filed, and NOT touched by this version) + 5 ordinary; editorial STOP-ORDINARY 0/4. ⭐ **All four v1.11 remediations HELD under both gates**, and the `AddValuation.top_iff` replacement was settled by ELABORATION in both directions: the `emultiplicity_eq_top` + `FiniteMultiplicity.of_prime_left` route compiles on ℤ_[2], and the control `AddValuation.top_iff` FAILS to elaborate there, as it must. Corrected here: the `AbstractSelfApp` and `AFAStructure` boxes still rendered Lean's `bot` as `⊥`, and there is no global `⊥` notation for `ZPSemilattice.bot` (only a LOCAL one), so compiling either box as displayed gave *failed to synthesize Bot L* — the v1.11 “transcribe literally” remedy reached half its own scope; the APG box showed `Reachable root v` where the field is `Nonempty (Quiver.Path root v)` and `Reachable` is `SimpleGraph`'s, not a quiver notion; “ℤ_[2] is NOT a ValuationStructure instance” shipped bare while three sources changed in the same commit hedge it, and the first correction fixed one clause of that sentence and left the sibling saying “no ZPSemilattice ℤ_[2] is DEFINED” — § V builds one — so BOTH now say what is checkable, that neither is DECLARED; `funext` was credited to `Fintype`, where function extensionality consumes no finiteness at all and finiteness is spent earlier on the acyclic descent (DC-32); and the note marker U+22EE, which means ELIDED MATERIAL, became the house U+2022. Every correction in this version was verified against the EXTRACTED PDF rather than the builder — which is how the round found them. ⚠ Recorded because it recurred: the verification needle itself failed twice on WHITESPACE — padded spacing, then a line wrap — and both times the document was right and the check was wrong (DC-22). Matching is whitespace-normalized now. v1.11: THE v1.10 REMEDY WAS APPLIED TO ONE BOX AND THE OTHERS KEPT THE DEFECT (adversary FAIL-BEDROCK, 4 bedrock; editorial STOP-ORDINARY, concurring independently on the fourth and compiling the failure). v1.10 established that a box which paraphrases is prose wearing a code block, and fixed `toAFAStructure` only. (1) Page 1 credited “`val_bot` and `val_scale` together” with giving finiteness for x ≠ ⊥. `val_finite_of_ne_bot` is `fun h => hx (val_unique x h)` — the contrapositive of `val_unique` ALONE — which `Scale.lean`'s module doc and `LEAN_CUSTOM_REGISTRY.md` already recorded, with a `Bool` counterexample. “The four axioms are minimal” fell with it: an axiom consumed nowhere on the argument is what minimality would deny. (2) § IV.1 said the decoration equation iterated k times gives d(v) = scaleᵏ(d(v)); `collect` reduces to `scale` only through `collect_singleton`, i.e. only at single-child vertices, and `cyclic_decoration_eq_bot` does not do this — it chains `collect_val_ge` and `path_val_chain` as INEQUALITIES and never forms `scale^[k]`. **The preamble restated the same overclaim in different words and was swept with it**, so this document no longer contradicts itself two pages apart. ⚠⚠ **BUT THE SWEEP WAS WITHIN THIS DOCUMENT ONLY, AND v1.11 FIRST CLAIMED MORE THAN THAT.** Adversary round 3 grepped the DELETED phrasing rather than the new one — DC-28's polarity trick — and found the same claim alive in `ZP-J_Self_Reference.pdf` p.13, `ZP-J_Illustrated_Companion.pdf` p.6, that document's traceability register (which grounds `cyclic_decoration_eq_bot` in `val_iterate + scale_iterate_unique_fp`, neither of which it uses), and two `APG.lean` docstrings. `ZP-J_Self_Reference.pdf` refutes itself one paragraph earlier, in its own `DecorationUniverse` box. **Those sites — enumerated immediately above, and filed as `ZPJ-ITER` — are OUTSTANDING and are not touched by this version**, whose scope is the addendum. The correct form of the claim is the dated one: searched across this builder, `register.md`, README, GUIDE and the corpus on 2026-09-01, with the other ZP-J documents added to the search on 2026-09-02 and found to carry it. (3) The source-files box called `ScaleBridge.lean` “ℤ₂ as ValuationStructure instance”; it declares `instZ2ValBridge : ValBridge ℤ_[2]`, and that file states twice that ℤ_[2] is NOT a `ValuationStructure`. (4) `AddValuation.top_iff` was cited as the standard name for the `val_bot` + `val_unique` pair one clause from a sentence about ℤ_[2]; `top_iff` is stated over a `[DivisionRing K]` and ℤ_[2] is a DVR, so it does not apply there — the stock route is `emultiplicity_eq_top` with `FiniteMultiplicity.of_prime_left`. The rest of that prior-art claim was verified and survives. Also: the `ValuationStructure` and `DecorationUniverse` boxes rendered Lean's `⊤` as `∞` and restated binders, so they did not typecheck as displayed, and are transcribed literally now; the set image `d '' children v` was encoded as U+201C followed by U+2032 at five sites — **invisible as a defect in the source and visibly broken on the page**; every box header and four rendered citations moved to full repository paths per R-LEANPDF; and this builder's own v1.9 changelog still carried the “both rendered into a deposited PDF” overclaim that `register.md` corrected the same day. Every correction listed above was verified against the EXTRACTED PDF, never the source. v1.10: THE RENDERED BOX PARAPHRASED THE LEAN IT CLAIMED TO SHOW. Found by the adversary gate reading the EXTRACTED PDF rather than the builder source. Three bindings in the toAFAStructure box were paraphrases, with selfMem shown as an anonymous lambda in Lean 3 comma syntax inside a Lean 4 corpus — so a reader could not resolve the def selfMemDerived citation v1.9 had just added against a box that never names it, and the citation fix was therefore only half delivered. Transcribed literally now, each field marked DATA or LAW. Separately, the prose called bot_self_mem "supplied", a word this section reserves for data, two paragraphs after drawing that distinction; it is a law discharged by a theorem and now says so. v1.9: CITE BY DECLARATION, NOT BY LINE. Both Lean citations in the builder were line numbers and BOTH had drifted: SelfApp.lean:83 pointed at AbstractSelfApp.fixed_bot and :113 at a proof body, neither at the definition being cited. A line number is a copy of a location, so it goes stale silently while reading as precise — the same defect one level down from the field-discipline claim it was citing. Cited as ZeroParadox/Computability/SelfApp.lean, def selfMemDerived. CORRECTED 2026-09-01: this entry originally said both citations "rendered into the deposited PDF". Only :113 did; :83 sat in the builder's module docstring, which reaches no rendered page. The overclaim was self-concealing, because the same edit removed the literal SelfApp.lean:83 text from both surfaces — it is named here again so the claim can be checked rather than taken.v1.8: FIELD-DISCIPLINE CORRECTION (bedrock). The abstraction-chain prose said "at each step, the fields of the target typeclass are proved as theorems from the source" and "inherits the full AFAStructure as a chain of theorems". False at BOTH steps: a Lean typeclass field is either a LAW you discharge with a proof or DATA you supply, and this chain supplies data at each step - selfApp := scale is an assignment (the box six lines below prints it), and selfMem is supplied by def selfMemDerived. TWO of AFAStructure's three fields become theorems, not three. This was the THIRD ZP-J surface carrying the claim; the other two were corrected in ZP-J v2.6 and comp v1.30 and this one was missed because its sentence contains no numeral. Found by editorial round 5 and now covered mechanically by tools/verify/check_fields.py, which tests the Lean binding rather than the wording.v1.7: Lean Source Files box lists SetTheoryAFA.lean (AFAStructure typeclass home); "seven"→"eight" source files; stripped "as of May 2026" dated qualifier from endnote.v1.6: rendered Lean citations synced to post-reorg files/namespaces (SSOT-driven).C8 dual-date templating (Initial/Current meta line; hardcoded month removed).v1.5: rendered Lean-file citations synced to post-reorg basenames (ZPJ_* -> *).v1.3: rendered self-version ref removed from endnote (C1 sweep); fixed scaleᵏ null glyph — formal:7e46e9e2|
| ZP-J Wheel Addendum | v1.7 | ZP-J_Wheel_Addendum.pdf | v1.6 | N/— | v1.7 / comp v1.6: THE DEGENERATE OVERLAP AND THE TERM-LEVEL IDENTIFICATION ARE RESTORED (two-pole audit 2026-09-13, Tim: restore all). v1.6 deleted "the one overlap is degenerate, stated rather than denied" alongside a genuinely false clause, reversing the 43116ba decision; the one-element algebra is again stated as both a wheel with ∞ = ⊥ and a model of the meadow equations, now carried by an rfl example in Wheel.lean § V. Both documents also keep that in a meadow the terms 0⁻¹ and 0·0⁻¹ both evaluate to 0, which no wheel with more than one element can do. v1.6: THE MEADOW REMARK'S IMPLICATION IS DELETED RATHER THAN RESTATED, and the companion is bumped with it. Five versions in a row tried to state a SCOPED implication in unscoped prose and all five failed — v1.0–1.3 "a wheel in which ∞ = ⊥ is exactly a meadow" (false); v1.4 "neither kind of meadow is a wheel with ∞ = ⊥" (vacuous); v1.5 "no NON-TRIVIAL meadow of either kind is…" (vacuous again, independently); and v1.5's own replacement "the implication runs meadow-equation to triviality, never the reverse" (FALSE). That last one dropped the binder: `Wheel.lean` §V scopes the hypothesis with `example {W : Type*} [Wheel W] (hm : winv 0 = 0) : ∀ x y : W, x = y`, and stripped of `[Wheel W]` the sentence asserts that satisfying the meadow equations makes an algebra trivial — false for ℚ and for every field, and contradicted by the Bergstra, Hirshfeld & Tucker definition quoted three paragraphs earlier in the same box. Its second clause was worse: "never the reverse" denied the sentence immediately before it, which had just asserted that the one-element wheel satisfies the meadow equations (the `Wheel.lean` gloss it was truncated from ends "— a one-element wheel need not have arisen this way", which makes that clause a claim about PROVENANCE; the PDF kept only the first half). So NO implication arrow about meadows survives in the rendered prose: both directions are cited to the machine-checked examples in ZeroParadox/Algebra/Wheel.lean §V, where the binders carry the scope, and the remark title no longer promises one either ("why it is not this" struck). The absence claim is now DATED and scoped to a named search set, and the Bergstra & Ponse null is attributed to COMMON meadows only, which is all they say. **comp v1.5: the biconditional the addendum struck at v1.5 was still live in the companion** — "The addendum proves exactly when that holds" claims a biconditional this corpus does not prove (only `inf_ne_bot (h0 : (0 : A) ∉ S)` is a declaration here; no `0 ∈ S → trivial` theorem exists anywhere in the corpus), and the converse was asserted flatly — now stated as one direction, with the converse cited to Carlström (printed p. 5). Found by reading the RENDERED companion against the RENDERED addendum: a source grep of the struck phrasing over the addendum returns only docstring hits, and `check_paths.py --claim` returned 0 sites over a domain that INCLUDED the companion, because the companion says it in different words. Also: `inf_ne_bot` relabelled Proposition (R-NAMING — page 1 calls it "the companion result"); `#print axioms zpw_top_val_iff_inv_is_inf` added to `Wheel.lean` §IX so the Axiom Footprint box's claim is reproducible from the file it cites; the dangling "the Lean encoding below" resolved to a full repository path; "porthole" glossed at first use in the companion; the endnote page break PINNED with `KeepTogether` (it was unheld, so any length change reverted it); endnote sorry-free survey re-dated; docstring months corrected in both scripts to match the rendered title blocks, which `zp_utils.version_line()` derives.v1.5: the v1.4 correction OVER-CORRECTED, shipping a universal negative that is false at every point of its domain — "So neither kind of meadow is a wheel with ∞ = ⊥, and neither arises by collapsing one." Prop. 4.4 makes the ONLY wheel with ∞ = ⊥ the one-element one, and that algebra IS a meadow of both kinds: the meadow axioms are purely equational, and Bergstra & Ponse state it in terms on the very page this remark already cited (1406.6878 §1 p. 2) — "we do not require a meadow to satisfy the separation axiom 0 ≠ 1." The forward direction is VACUOUS, not false. Wheel.lean §V already carried the right qualifier ("NON-TRIVIAL involutive meadow") and the rendered prose had dropped it. (A PUnit witness was cited here for that overlap; it is NOT in the tree — no `Wheel` instance on `PUnit` exists anywhere in the corpus, verified 2026-09-13. The overlap argument stands on the equational form of the meadow axioms, which is quoted; the machine-checked claim did not.) Now quantified over non-trivial meadows, with the degenerate overlap stated rather than denied. Three more folded in under the same bump: "The originators say as much" struck (not-yet-found is not not-there, and the quote is Bergstra & Ponse's, who originated COMMON meadows, not meadows); THE ENDNOTE GENERALISED A SCOPED FOOTPRINT — re-measured, instWheel and inf_ne_bot are [propext, Quot.sound], zpw_inf_ne_bot is [propext], and zpw_inv_zero_eq_inf, zpw_zero_mul_inf_eq_bot and zpw_top_val_iff_inv_is_inf each carry Classical.choice (the last of them named one page earlier as "proved directly"), so the Axiom Footprint box now states its own exceptions and the endnote defers to it; "if S contains a zero divisor witness for 0" was garbled and false on its natural reading (in ℤ/6, S = {1,2,4} is a submonoid of zero divisors with 0 ∉ S), so the witnesses s = 0, s′ = 1 are now named; and "exactly when 0 ∈ S" implied a biconditional this corpus does not prove — only 0 ∉ S → ∞ ≠ ⊥ is a declaration here, so the converse is cited to Carlström (p. 5) rather than implied to be ours. Companion left at v1.4 on the claim that "it carries none of these" — FALSE, and retracted at v1.6: it carried the biconditional, in different words.v1.4: wheel/meadow claim corrected (bedrock, prior-art gate) — struck "a wheel in which ∞ = ⊥ is exactly a meadow", "a wheel collapses to a meadow" and the companion's "both answers give a consistent algebra". Identifying ∞ with ⊥ forces the ONE-ELEMENT wheel (Carlström 2001:11 Prop. 4.4, p. 25; machine-checked over the Wheel typeclass in ZeroParadox/Algebra/Wheel.lean §V), and a meadow adjoins no element at all — "a commutative ring with a total inverse operator satisfying two equations which imply 0⁻¹ = 0" (Bergstra, Hirshfeld & Tucker, arXiv:0901.0823); a COMMON meadow adjoins one absorbing element and is non-involutive, so W7 fails (Bergstra & Ponse, arXiv:1406.6878, who also record that no structural connection to wheels is known). Credit corrected: the name and construction are Setzer's, Carlström generalizes to any commutative SEMIRING. comp v1.4: diagram is now three panels (wheel / meadow / collapse); ⊙ etymology corrected to the topological picture. C8 dual-date templating (Initial/Current meta line; hardcoded month removed).v1.3: rendered Lean-file citations synced to post-reorg basenames (ZPJ_Wheel/WheelFrac -> Wheel/WheelFrac).v1.1: WheelFrac.* citations → ZPJ_WheelFrac.* (Lean namespace standardization); comp v1.1 same. v1.0: Initial release — wheel of fractions ⊙_S A = (A×A)/≡_S is a Wheel (Carlström Def 1.1, 14 fields), choice-free [propext, Quot.sound]; inf_ne_bot porthole (∞≠⊥ given 0∉S). Lean: ZPJ_WheelFrac.lean; fix() guard added (PDF build standards). comp v1.0: ZP-J_Wheel_Illustrated_Companion.pdf — plain-language (division-by-zero made total; ∞=/0, ⊥=0·/0; wheel-vs-meadow diagram; instWheel + inf_ne_bot; porthole connection) — formal:014fe9b9 comp:5f817977 |
| ZP-J Keystone Addendum | v1.13 | ZP-J_Keystone_Addendum.pdf | N/A | N/— | v1.13: ATTRIBUTION, PRIOR-ART GATE (PA-1, 2026-09-15): the prior-art paragraph credited Lawvere (1969) with unifying Cantor, Russell, Gödel "and the recursion theorem". Lawvere does not treat it — his own introduction names Cantor, Russell, Gödel and Tarski, and the TAC reprint contains no occurrence of Kleene, Rogers or "recursion". Lawvere's list now ends at Tarski's undefinability theorem; Turing's halting argument and the recursion theorem move to Yanofsky (2003), who proves the latter as his Theorem 5. Matches README.md:21 and CLAIMS.md:145. Recurrence of ATTR-2, whose 2026-08-29 closure claimed a corpus-wide sweep found no further instances — retracted; this site was invisible to that sweep because it never names Kleene. v1.12: DA-1/KLEENE CLASS, GATE ROUND 4 SECOND PASS (Tim ruling, 2026-09-15): the 'What the Zero Paradox adds' box names the proved faces (Quine atom, 2-adic, categorical; the Kleene face carried as a requirement). v1.11: DA-1/KLEENE CLASS, GATE ROUND 4 (Tim rulings, 2026-09-15): the preamble and Section II name which faces' MC-1 membership is proved (Quine atom, 2-adic, categorical); the Kleene face is carried as a requirement, its witness computability_face_fixedPoint not tied to bottom. Missing space after 'framework:' restored. v1.10: DECISION BATCH REMEDIATION AFTER GATE ROUND 3, SECOND PASS (Tim ruling, 2026-09-15): the preamble's 'bottom is the same self-referential (diagonal) fixed point in every framework' is 'bottom plays the same self-referential (diagonal) fixed-point role in every framework'. v1.9: DECISION BATCH REMEDIATION AFTER GATE ROUND 2 (Tim rulings, 2026-09-15): both MC-1 sites retire the identity because object equality across categories does not typecheck and is not invariant under equivalence; the seam_unique_among_named property clause is dropped, its named bottoms not being this addendum's faces. v1.8: DECISION BATCH REMEDIATION ROUND 2 (Tim rulings, 2026-09-15): two sites said the MC-1 members are 'provably distinct'; now the MC-1 identity is retired as ill-typed (neither x = y nor x != y is well-formed across categories) and the members differ by property, each in its own category (seam_unique_among_named). v1.7: BEDROCK, the same mechanism defect as ZP-R v1.6, rendered here too. The escape was given as eval landing "in the partial functions, not in the codes, so the Set refutation never applied to it". Refuted by elaboration - the partial-function type is nontrivial, so the Set refutation lands on it as well. The escape is that the effective category admits fewer MORPHISMS: no computable self-map on codes is eval-fixed-point-free, so the refuting diagonal has no computable representative. Prior art unchanged; only the stated reason was wrong. v1.6: ROGERS-1 — the up-to-eval qualifier. The document said "every computable self-map on codes has a fixed point", bare. That is FALSE as stated: c ↦ pair(c,c) is total, computable and literally fixed-point-free (both facts elaborated). Rogers gives a fixed point only UP TO eval — two codes computing the same function. The escape from the Cantor obstruction survives but for a different reason than the text gave: eval lands in the partial functions, not in the codes, so the Set refutation never applied. Found by the adversary gate 2026-08-17 after the same claim had been corrected three times elsewhere; this surface was missed because the sweep keyed on the DECLARATION NAME being renamed while a statement-identical theorem is cited under a different name (DC-24; the .lean-versus-.py diagnosis was falsified 2026-08-18). v1.5: THE v1.4 FIX WAS PARTIAL, WHICH MADE IT WORSE. v1.4 corrected the two sites carrying the forbidden PHRASES and missed a third stating the same claim in other words ("The one-object identification remains the MC-1 commitment"), leaving the document saying the identity was retired on page 1 and live on page 2 - before v1.4 it was uniformly stale, i.e. self-consistent. A partial fix to a self-consistent error manufactures a self-contradiction. Both gates returned FAIL-BEDROCK. A FOURTH site then surfaced, flagged by neither gate, found only by sweeping the RENDERED text for the CLAIM (any sentence pairing an identity notion with a live-status verb) rather than for phrases: section III called MC-1 an "existing identification" and its "bottom/epsilon-zero identification" wording could be read as equating the endpoints, which epsilon0_ne_bot forbids; now a role assignment with the distinctness named. Endnote Lean sources upgraded to full repo paths.v1.4: MC-1 FRAMING brought to the ratified form (KEY-1, a release blocker). THREE rendered sites (v1.4 fixed two, v1.5 the third) called the cross-category identity a live "modeling commitment" and said it was "offered" - that identity was RETIRED AS ILL-TYPED (an equation across distinct categories is not a well-formed proposition), and CLAIMS.md already said so, so the PDF disagreed with the ledger. Now: family membership proved per domain, criteria a design principle, identity retired, members provably distinct. Also KEY-2: Cor 8.2 lists FIVE equivalent conditions (the docstring had said "three-way"); the category/functor split restated unambiguously (smooth-monos and subobject-classifier are the CATEGORY's; preserving inverse images and the pre-fixed point are the ENDOFUNCTOR's; routes not exhaustive); prior-state prose narrating an unshipped draft removed from the docstring, which ships publicly via scripts/. R2-4 (Taylor cited page-precisely with no year or venue) NOT fixed - the on-disk copy carries neither and inventing them is worse than the gap.v1.3: CITATION SCOPE - section III called the biconditional "the General Recursion Theorem"; AMM Thm 7.2 p.27 is the FORWARD direction only, their section 8 is its converse. That converse asks the ENDOFUNCTOR to preserve inverse images plus one of SEVERAL routes (Thm 8.1 smooth-monos+pre-fixed-point / Thm 8.6 subobject classifier / Thm 8.12 vector spaces) - the first two conditions are the CATEGORY's, not the functor's, and they are not exhaustive; Cor 8.2 gives the equivalence under Thm 8.1's assumptions. Taylor's necessity scoped as he scopes it, IN A TOPOS (Prop 111 p.6, stated not proved), forward half at Thm 36 p.15; the "cannot recurse through the bottom" reading marked as this framework's gloss. Next-time operator no longer listed as missing (built at ZeroParadox/Category/NextTimeCategorical.lean); remaining Mathlib absences dated, not asserted.v1.2: rendered Lean-file citations synced to post-reorg basenames (ZPJ_* -> *).v1.1: honest-scope precision — the single-carrier "snap is one crossing" carries no NEW commitment; it rests on the framework's existing ⊥/ε₀ identification (MC-1 / OQ-E2), endpoints proved (floor non-wf via real ⊥, axiom-free). v1.0: Initial release — thin addendum recording two machine-checked keystone probes. (1) Lawvere face-split (ZPJ_Lawvere.lean): in Set no face is a Lawvere instance (Cantor — nontrivial_lattice_no_witness, q2_no_witness), the computability face genuine (computability_face_fixedPoint, wraps Mathlib recursion theorem); fixedPoint_of_witness / no_witness_of_fixedPointFree axiom-free. (2) Well-foundedness boundary (ZPJ_Boundary.lean, ZPJ_BoundaryBridge.lean): the snap as a ν→μ crossing — relation level (floor_not_wellFounded axiom-free; snap_crossing) + QPF bridge (snap_boundary_two_registers), best-effort; full Taylor coalgebraic (next-operator / Pataraia / General Recursion Theorem) open, cited to Taylor/AMM. Honest fences throughout (probe; no-new-commitment; open Rung-C) — formal:840acb6d |
| ZP-K Computational Grounding | v1.23 | ZP-K_Computational_Grounding.pdf | v1.21 | N/— | v1.23 / comp v1.21: ZPK-BED-1 AND ZPK-BED-3, BEDROCK IN DEPOSITED PDFs (Tim ruling, 2026-09-19). ⭐ THE MEASUREMENT CORRECTS SECTION IV ITSELF, not only the defect sites: the axiom does not ride on the TYPE. `Nat.Partrec.Code` is axiom-free, `Nat.Partrec.Code.eval` is axiom-free, `IsKleeneFixedPoint` (type `eval c = f c`) is axiom-free, and a theorem that RUNS a program reports `[propext, Quot.sound]` with no choice — while `encode c = encode c`, proved by `rfl` and doing no work, reports the full triple. The axiom enters through Mathlib's `Denumerable`/`Encodable` instance reached by `encode`: an OPERATION, not a type. § IV said "statements that mention program codes" and now says "names a program's index". § I's "all ZP-K theorems carry … shared by all Mathlib computability results" now POINTS at § IV. ⚠ Precisely: the SECOND universal is false as measured (a theorem using Mathlib's computability library can be axiom-free), while the FIRST is true of theorems — 19 of 19 in `Kleene.lean`, the two axiom-free entries there being a `def` and an `instance`. What was unsupported is the causal "is why", which attributed the footprint to the non-constructive existence rather than to the operation that carries it. § II chained a false premise — distinct CODES have distinct Gödel numbers "so" each generates a fixed point with a distinct period; injectivity presupposes plurality and cannot produce it, so only the infinitude is given and the Gödel number is named as A period, not the least. Companion: "the same dependency carried by any theorem using Mathlib's computability library" is the same refuted universal and now points at § IV; and "IVT depends on completeness of the reals, which depends on choice" is removed — Dedekind completeness is a ZF theorem. ⚠ THE COMPANION WAS JUDGED "not materially stale" IN AN EARLIER ROUND OF THIS WORK AND THAT JUDGEMENT WAS WRONG; it was caught by sweeping the DEPOSITED companion PDF. ⚠⚠ § IV NOW HOLDS A DATED MEASUREMENT TABLE AND NO GENERAL RULE (Tim ruling, 2026-09-19). Five successive general rules were written about this one `Classical.choice` and all five were false. The last two died here: *"a theorem that runs a program carries no choice"* is refuted by `roger_fixed_point_exists` (full triple), and *"a statement that names a program's index carries the axiom"* by `encodeCode c = encodeCode c` — `rfl`, **axiom-free** — against `Encodable.encode c = Encodable.encode c` — `rfl`, full triple. Two spellings of the same Gödel number, opposite footprints, Mathlib proving `encode = encodeCode`. **A centre holding a rule hands its error to every document pointing at it; a centre holding a measurement inherits nothing.** The table is DATED and tells the reader to re-run rather than cite it, because `ChoiceCannotBe.lean`'s "No count is recorded here" records this project getting a cited figure wrong three times. Measurements and post-mortem: `.claude-local/notes/axiom_footprint_measured_2026-09-19.md`. v1.22: SET-THEORETIC CLAUSE DELETED (Tim ruling, gate round 6, 2026-09-15): Section IV said 'The order-theoretic and set-theoretic results are unaffected.' IsQuineAtom (bot : MachinePhase) is a set-theoretic result whose Lean theorem da1_closed_concrete lists Classical.choice through machinePhaseKleene; the sentence is deleted, and 'The classical axioms are entirely localised to the computational layer.' is kept. Companion reviewed, unchanged. v1.21: CLASSICAL.CHOICE PROVENANCE, SCOPED (Tim ruling, gate round 6, 2026-09-15): Section IV said all ZP-K theorems' axioms enter through the types; da1_closed_concrete's statement IsQuineAtom (bot : MachinePhase) depends on no axioms, its choice comes through its proof at machinePhaseKleene, and bot_is_quine_atom proves the same statement with no axioms. The body now says most ZP-K theorems carry the axioms, scopes the types sentence to statements that mention program codes, and names da1_closed_concrete as the exception; the remark's opening is scoped the same way; the da1_closed_concrete box's Purity line names the axiom-free proof. Companion reviewed, unchanged. v1.20: CLASSICAL.CHOICE PROVENANCE, SECTION IV BODY (Tim ruling, gate round 5, 2026-09-15): 'These enter exclusively through Kleene's theorem and Rogers' theorem' now says that in the computability layer these enter through the types, via Mathlib's numbering of program codes (Denumerable Code), which Kleene's theorem and Rogers' theorem both use (a constant-code instance uses neither theorem and carries Classical.choice; fixed_point and fixed_point2 both list it). Companion reviewed, unchanged. v1.19: CLASSICAL.CHOICE PROVENANCE (Tim ruling, gate round 5, 2026-09-15): the classical-choice remark said the choice belongs to this instance because a computable constant-code instance exists; that fact is about the noncomputable marker. Classical.choice is carried by the statements' types through Mathlib's numbering of program codes (Denumerable Code) and is present in the computable instance too; Classical.choose is what makes machinePhaseKleene noncomputable; whether the numbering's footprint is essential is not measured. Companion reviewed, unchanged. v1.18 / comp v1.20: DA-1/KLEENE CLASS, GATE ROUND 4 (Tim rulings, 2026-09-15): executor sentences labelled the framework's reading at the sentence; the T-COMP box lists t_comp's three conditions and states botCode existence separately as the KleeneStructure requirement; the classical-choice remark says the choice belongs to this instance (a computable constant-code instance exists); 'vacuously' is 'trivially'. Companion: 'It proves that there is a fourth description' now says the computational face is a KleeneStructure requirement; 'Now IN LEAN SCOPE via ZP-K' states what Lean witnesses for Path 1. v1.17: DA-1 READING FENCED, THIRD PASS (Tim rulings, 2026-09-15): the description_instantiation_gap_closed box, the da1_closed_concrete Interpretation and the Section II body now mark bottom (c0) as read as the executor / the universal Turing machine in its ground state, the framework's reading and not a Lean theorem; Lean proves only the Quine-atom statement. Companion reviewed, unchanged. v1.16 / comp v1.19: DA-1 PATH 3, SECOND PASS (Tim rulings, 2026-09-15): headings 'DA-1 Formally Closed' / 'DA-1 Closed' are 'DA-1: what Lean witnesses', the da1_closed_concrete box title is 'the Path 1 witness', and the description_instantiation_gap_closed box says bottom is read as the executor, Lean witnessing the AFA Quine atom (da1_closed_concrete) and carrying the Kleene quine as a KleeneStructure requirement, the one-fact reading the framework's. v1.15 / comp v1.18: DA-1 PATH 3 (pre-existing bedrock, editorial gate round 3 B1, 2026-09-15): the internal contradiction 'ZP-K resolves Paths 1 and 3' / 'Path 3 is NOT resolved here' is removed, with 'Paths 1 and 3 are formally closed / closed' and 'projections of one structural identity'; the companion's 'ZP-K closes DA-1 Paths 1 and 3 formally' box likewise. Each site carries the CLAIMS.md DA-1 row. v1.14 / comp v1.17: "Rogers' fixed-point theorem" corrected from "Roger's" (Hartley Rogers Jr.). ZP-L made this exact correction at its v1.4 and it was never swept to the rest of the corpus; Mathlib carries the same typo upstream at Computability/PartrecCode.lean:36,1001. Prose only, no claim changed.v1.13: BEDROCK - the class-field-as-theorem root cause propagated to six sites. botCode and botCode_is_quine are now named as a CLASS FIELD carrying a periodicity condition that constant codes also satisfy, not as a witness that "botCode IS its own program"; the preamble no longer says Kleene's theorem "provides the formal witness"; Section III and the verification table said "four-way equivalence" where t_comp proves THREE. R-K.0 gains the type-level statement of the gap: (1)-(3) are properties of an element of L, (4) of a Code, with no function between them anywhere in the development.v1.12: witness audit - bot_self_mem_from_kleene is a RESTATEMENT of the inherited AFAStructure field, not "the Kleene side implies the AFA side"; the two conditions are class FIELDS and their sameness is the framework's reading, the motivation for the class rather than something derived in it.v1.11: DA-1 Path 3 RECLASSIFIED (Tim, 2026-07-27) from CLOSED / IN SCOPE to FOUNDATIONAL COMMITMENT, matching Path 2 and CLAIMS.md. Its witness botCode_is_quine is a class field assumed at instantiation, not a second independent proof.v1.10: T-COMP overclaim corrected (bedrock) - preamble no longer says the four roles "are shown to be the same structural object" (three PROVED to coincide; the computational role is a KleeneStructure typeclass field, as R-K.0 already stated); footer chained cross-type identity replaced; "DA-1 closed" narrowed to "DA-1 structural half". Companion v1.13 same class.v1.9 / comp v1.12: rendered Lean citations synced to post-reorg files/namespaces (SSOT-driven).C8 dual-date templating (Initial/Current meta line; hardcoded month removed).v1.7: version changelog removed from preamble; palette rebuild. comp v1.10: four_way_diagram — removed the redundant internal caption String that overlapped the "Computation (Kleene)" box (Diagram Rule 4). comp v1.9: FMC precision (sweep Step 4) — DA-1 Path 1 splits the axiom-free structural fixed point from the literal ⊥={⊥} (ZF+AFA setting) — formal:e12e6b3d comp:2e1fc531 |
| ZP-L Incomputability Convergence | v1.13 | ZP-L_Incomputability_Convergence.pdf | v1.8 | N/— | v1.13 / comp v1.8: ZPK-BED-2, BEDROCK IN A DEPOSITED PDF (Tim ruling, 2026-09-19). Sites in this document and its companion asserted one axiom footprint across all the settings, and the § I table refutes that: its ZPJ/K cell names `bot_self_mem` (AFA), which measures no axioms, beside `botCode` (Kleene), which carries them. "Required" is a necessity claim no `#print axioms` run can earn — necessity takes a reduction to a taboo (`ChoiceCannotBe.lean` § IV) — and "a constructive alternative was not found" is false, the alternative being the row's own AFA witness. Sites here POINT at ZP-K § IV, which holds a dated measurement table rather than a rule (Tim ruling, 2026-09-19). Also removed: the "Why K is Absent from Lean" REASON (uncomputability does not explain absence from Lean — the corpus carries ~200 `noncomputable` declarations, two of them in `Gentzen.lean` itself), a theorem count that counted table ROWS, and "24 theorems proved" on page 1. Prose no longer enumerates the settings (Tim ruling) — the table defines them, and three prose sites had enumerated them three different ways while every claim was quantified over the set. The table is unchanged, per the ruling. Companion: the mechanism clause "the Kleene proof uses it for the fixed-point selection" and its twin in the two-rooms box are DELETED (a mechanism ZP-K v1.20 had already retracted); the Kleene identification is stated as the framework's reading, not a Lean equation; and both claims that ZPM formalizes a type bridge between the settings are corrected — `snapEmbed` is `MachinePhase → ℤ₂`, so ZPM relates the two ENCODINGS, and a bridge between the settings is open. ⚠ SCOPE OF THE SEARCH, stated instead of a count (R-NOTINLIB): `check_paths.py --full --claim` over `.md` + `.lean` + tracked `.py` + the 40 rendered PDFs, phrasings varied by POLARITY, PART OF SPEECH and VOCABULARY. v1.12: CLASSICAL.CHOICE MODAL (Tim ruling, gate round 5, 2026-09-15): the Axiom Purity box's 'expected and documented, not incidental' drops 'not incidental', a necessity claim beside 'essential is not measured'. Companion reviewed, unchanged. v1.11: CLASSICAL.CHOICE PROVENANCE (Tim ruling, gate round 5, 2026-09-15): the Axiom Purity box said the computability-layer choice belongs to ZP-K's instance; it now says Classical.choice is carried by the statements' types through Mathlib's Denumerable Code, present even in a computable constant-code instance, that Classical.choose makes machinePhaseKleene noncomputable, and that essentiality is not measured. Companion reviewed, unchanged. v1.10: DA-1/KLEENE CLASS, GATE ROUND 4 SECOND PASS (Tim rulings, 2026-09-15): Section II names Rogers' fixed-point theorem as Mathlib fixed_point, inter-derivable with fixed_point2, for a total computable transformation; the verification box says the computability-layer choice belongs to ZP-K's instance. Companion reviewed, unchanged. v1.9 / comp v1.7: DA-1/KLEENE CLASS, GATE ROUND 4 (Tim rulings, 2026-09-15): the overview names Rogers' fixed-point theorem (Mathlib fixed_point, total computable transformation) as inter-derivable with Kleene's second recursion theorem rather than equal to it; 'via a provable path' is 'via a path whose Kleene step is a KleeneStructure requirement'. Companion: 'ZP-K proved that c0 is a Kleene fixed point' now says ZP-K carries that face as a KleeneStructure requirement. v1.8: DECISION BATCH REMEDIATION AFTER GATE ROUND 2 (Tim rulings, 2026-09-15): the surreals note sits where the snap is proved impossible; its occurrence clause is removed. v1.7: DECISION BATCH REMEDIATION ROUND 2 (Tim rulings, 2026-09-15): the surreals note's 'occurrence is a framework commitment' now says the Snap occurring follows from the occurrence commitment (instantiation occurs) together with DA-1 (closed given DP-2). Companion reviewed, unchanged. v1.6: FORCING OVERCLAIM RETRACTED. The document called the snap "the forced transition" with no occurrence hedge anywhere. T-SNAP fixes the transition's SHAPE; tsnap_holds_but_nothing_moves proves it holds in a model where nothing moves, so occurrence is a framework commitment. Prose only.v1.5: axiom-footprint list label corrected - t_comp is a three-way equivalence, not four (the computational face is an assumption, not a clause). Footprint figures unchanged.v1.4: "Rogers' fixed-point theorem" corrected from "Roger's" (Hartley Rogers) — Section II heading + prose.v1.3 / comp v1.6: rendered Lean citations synced to post-reorg files/namespaces (SSOT-driven).C8 dual-date templating (Initial/Current meta line; hardcoded month removed).v1.1: rendered version changelog removed + 3 null glyphs fixed (subscript-letter entities ₙ/ₒ → <sub> markup) (C1 sweep); comp v1.4: strip footer version — formal:c01cec20 comp:1169e4b5 |
| ZP-M Kleene-Ordinal Bridge | v1.7 | ZP-M_Kleene_Ordinal_Bridge.pdf | v1.6 | N/— | v1.7: ZPK-BED SWEEP (Tim rulings, 2026-09-19). The Axiom Purity box carried the THIRD rendered statement of the axiom-provenance rule — *"Classical.choice is carried by the statements' types"* — which ZP-K § IV stopped stating the same day, five successive general rules about this one `Classical.choice` having all been measured false. It now names only the mechanism it can back (the `Denumerable Code` instance) and points at § IV's dated measurement table. ⚠ The box's *"Classical.choice is load-bearing in the ordinal fixed-point (nfp)"* is UNTOUCHED and is NOT a defect — licensed by `ZeroParadox/Ordinal/OrdinalChoiceEssential.lean`; a reviewer filed it and WITHDREW it, and the withdrawal is the recorded lesson. Located by an axis-varied sweep of the RENDERED PDFs; two earlier gates saw the site and correctly scoped it out, and it is in scope here by the same ruling that brought the Foreword in. Companion reviewed, unchanged. v1.6: CLASSICAL.CHOICE PROVENANCE (Tim ruling, gate round 5, 2026-09-15): the Axiom Purity box said the choice in the computability fixed point belongs to ZP-K's instance; it now says Classical.choice is carried by the statements' types through Mathlib's Denumerable Code, present even in a computable constant-code instance, that Classical.choose makes machinePhaseKleene noncomputable, and that essentiality is not measured. The ordinal half is unchanged. Companion reviewed, unchanged. v1.5: DA-1/KLEENE CLASS, GATE ROUND 4 SECOND PASS (Tim ruling, 2026-09-15): the verification box says the choice in the computability fixed point belongs to ZP-K's instance, a computable constant-code instance also existing; the ordinal half is unchanged. Companion reviewed, unchanged. v1.4 / comp v1.6: DA-1/KLEENE CLASS, GATE ROUND 4 (Tim rulings, 2026-09-15): c0 is described as read as the Kleene quine, a KleeneStructure requirement in ZP-K. Companion: 'ZP-K proved that c0 is a Kleene fixed point' and 'they were unified in ZP-K' now carry the requirement and the conjunction-of-witnesses reading (da1_paths_unified). v1.3 / comp v1.5: rendered Lean citations synced to post-reorg files/namespaces (SSOT-driven).C8 dual-date templating (Initial/Current meta line; hardcoded month removed).v1.1: rendered version changelog removed from title note + endnote (C1 sweep); v1.0: snapEmbed type bridge, hfp gap closed, zpm_triangle, R-M.1; comp v1.3: rendered self-version removed from Key Results header (C1) — formal:589cbf08 comp:d2106aba |
| ZP-N The Constructive Snap | v2.0 | ZP-N_The_Constructive_Snap.pdf | N/A | N/— | v2.0 (2026-07-20): Major revision — corrects v1.0's mechanism and adds the construction it was missing. The ascent is constructive (unchanged, still the layer's genuine content): exp_lt_term, omegaPow_no_fixedpoint, tower_strictMono, all [propext] only. NEW: (a) the classical dependency is located and shown load-bearing — comparability of arbitrary well-orders implies excluded middle (em_of_wellOrder_comparable, [propext, Quot.sound]); the taboo is Kraus/Nordvall Forsberg/Xu arXiv:2104.02549 Thm 38(d), cited not claimed, and is a PAPER proof (no machine-checked version located). (b) the footprint instrument is shown uninformative for ε₀ results — choice sits in the Ordinal.partialOrder INSTANCE TERM, so a ≤ a carries it and a = a does not (order_footprint_le / order_footprint_eq). (c) a carrier sized to the job — E0Note = WithTop ONote, crossing into Ordinal via one named map e0Repr; carrier choice-free, crossing carries Classical.choice at every decl; construction credited to Castéran (hydra-battles ON_plus/ON_correct carry his name alone; the library is his and Contejean's), ON_correct NOT claimed - only the first of its three fields is established (e0Repr_not_injective), top fixed point STIPULATED. Whether ZP-L's ε₀ results are eliminable remains UNCLASSIFIED. Minimality (ε₀ the LEAST fixed point) open. Lean: ConstructiveOrdinals.lean, SnapNucleusConstructive.lean, OrdinalChoiceEssential.lean, PricedInterface.lean — formal:5011bb68 |
| ZP-P The Fixed-Point Fork | v1.24 | ZP-P_The_Fixed_Point_Fork.pdf | N/A | N/— | v1.24: “choice enters only in the analytic realisations” was a universal negative, and false. Measured: `fixedPointFree_of_nontrivial` takes its choice from a bare `classical` in FRAMEWORK source (`Category/Lawvere.lean`), not from Mathlib, while its sibling `no_witness_of_fixedPointFree` is axiom-free — so the dependence is not inherited (the `classical` is the framework's own). ⚠ Do NOT read the axiom-free sibling as evidence of avoidability: it consumes a fixed-point-free map where the classical one CONSTRUCTS it, and `LawvereTaboo.lean` § III shows the cost IS the generality over arbitrary types - it disappears under `[DecidableEq β]` and cannot otherwise. Two choice-free taboo reductions (`wem_of_fixedPointFree`, `em_of_wellOrder_comparable`, both `[propext, Quot.sound]`) settle necessity for the principles they cover, which no footprint measurement ever could. v1.23: two defects in ONE rendered p.4 sentence, both from the v1.22 fix. (1) The restored carrier phrase went in at the wrong POSITION here while all eight Lean sites got the clean order, so the publication site read "with no axioms over that M-type" — nearest-head parsing gives something meaningless. (2) The dangling pronoun was REPORTED as fixed in v1.22 and was not touched: "The M-type FORMER and constructors are axiom-free, ITS DESTRUCTOR ...". Both corrected; the checkable sites and the published one now read identically. Worth recording the shape: the sites a gate can grep got the good wording and the one that renders got the degraded one.v1.22: the v1.21 CLASS sweep missed a member, and said it had not. (1) It claimed to have reached every site; `ChoicePurityInvariant.lean` wrote the phrase as `is **axiom-free**` — markdown bold INSIDE the phrase — so the literal pattern did not match, in a file that same commit edited twice. Fixed, and v1.21's claim corrected to "the live sites" because it was false as written. GREP LOOSELY: markup inside a phrase defeats a literal pattern. (2) The sweep also DELETED a load-bearing carrier phrase — `strict_cofix_nonempty` is over the M-TYPE, not over `Cofix`, so "proves the same inhabitation" took `cofix_nonempty` as its nearest antecedent and read as contradicting its own preceding clause; "over the M-type" restored everywhere including the rendered p.4 box. (3) Proposition 9 is in the paper's section 2, which one sentence still contradicted 25 lines above the fix. All three caught by gate round 1.v1.21: swept the CLASS the v1.20 fix left behind, plus the widow. (1) The discredited WARRANT "PFunctor.M is axiom-free" survived at 13 live sites after v1.20 removed the false CONCLUSION it supported — one Lean file carried the precise account and the loose one 55 lines apart. It is true of the TYPE FORMER and silent about the ELIMINATORS (M.children / M.dest carry the axiom), so the live sites now state former / constructors / destructor separately. (2) The Theorem Summary table orphaned cofix_nonempty alone on p.6 under a repeated header, reported by three consecutive gate rounds and surviving three rebuilds; it now starts on its own page, since nudging spacing only changes which row is orphaned.v1.20: BEDROCK — the v1.19 sweep fixed the two-page contradiction and swept to a FALSE statement. It said the choice is "not from the M-type underneath", warranted by "PFunctor.M is axiom-free". That is a witness-vs-statement defect: PFunctor.M is the TYPE FORMER and its purity says nothing about its eliminators. Measured: PFunctor.M / .mk / .corec are axiom-free, but M.children and M.dest carry [propext, Classical.choice, Quot.sound], and QPF.Cofix inherits from them via Mcongr/IsPrecongr — so the choice DOES come from the M-type, from its destructor. The true account is stronger and is now what the remark box says: strict_cofix_nonempty is axiom-free because it only BUILDS (M.corec) and never DESTRUCTS. Caught by the gate, which measured the claim rather than reading it.v1.19: the v1.18 fix swept ONE of the sentence's sites. Two more rendered — p.4's result box and p.6's Axiom Purity box both still attributed the choice to "the M-type / corecursion machinery", so the published document said "not from the M-type underneath" on p.4 and "from the M-type ..." on p.6, contradicting itself across two pages. Both now say QPF corecursion / quotient layer, which is what the measurement supports (QPF.Cofix carries the axiom in the type; PFunctor.M is axiom-free). Caught by gate round 2, which correctly ruled it ordinary: QPF.Cofix IS the quotient of PFunctor.M, so the loose name was imprecise rather than false and no conclusion rested on it.v1.18: the v1.17 clarification was PREPENDED and the original attribution left standing, so the remark box read "inherited from Mathlib's M-TYPE MACHINERY ... not the M-TYPE underneath" — one sentence contradicting itself. Same prepended-hedge shape as v1.10, and the seventh time this document has produced it. Rewritten once, attribution first: the choice comes from the QPF quotient layer, QPF.Cofix carries it in the type, PFunctor.M is axiom-free. Caught by gate round 1 on the modal-claim sweep.v1.17: MEASURED, replacing an inference. Two rendered sites called the nu-side Classical.choice "a library artifact, not a necessity" and "not forced by the mathematics". True in spirit, unmeasured as stated. Measured 2026-08-03: QPF.Cofix carries Classical.choice IN THE TYPE, so no proof of any Cofix-mentioning statement is choice-free — it is not removable by rewriting. What makes it an artifact is the pair: PFunctor.M is axiom-free and strict_cofix_nonempty proves the same nu-inhabitation over it with NO axioms, so the choice belongs to the QPF quotient layer and escaping it means changing the carrier. Found by the new modal-claim sweep (.claude-local/check_modal.py).v1.16: THE SENTENCE IS DELETED (Tim). Two remark-box bullets characterised the choice modality of Veltri's finite-powerset presentations. That material was wrong in SIX consecutive versions (v1.9 a universal, v1.10 a doubling, v1.11 the universal restored, v1.13/v1.14 a false universal, v1.15 a false uniqueness), each fix locally reasonable and each seeding the next. It was never load-bearing here: ZP-P's claim is that the Classical.choice in cofix_nonempty is a Mathlib artifact for a POLYNOMIAL functor, which bullet 1 states and ACS supports. The finite-powerset material is now one sentence saying it is a different case, out of scope, and recorded in ZeroParadox/Settheory/Coalgebra.lean, where the gates have verified it against the source. Prose that cannot be kept correct across six attempts does not belong in a published PDF when an accurate statement of it already exists in a checkable file.v1.15: BEDROCK — the v1.14 fix replaced a false UNIVERSAL with a false UNIQUENESS. “not all of them use it, and the exception is the one he prefers”: Veltri has TWO choice-free finality results, not one — Theorem 1 (the setoid presentation, final in SetoidRel, proved by anaTree with no classical hypothesis) as well as Theorem 2 (the coinductive type). His stated reason for preferring (ii) over (i) is ergonomic, that it “does not force the user to employ setoids instead of types”, which is exactly what “the exception” erased. FIFTH consecutive version in which this one sentence has been wrong; the quantifier is deleted rather than re-hedged and both theorems named. The truth was STRONGER than the claim, so the fix cost nothing. Also: Propositions 59 and 60 relocated to the paper's section 8 (Approximating Subtraction, Division and Logarithm Operations) — they are not in the taboo section 7 — and the orphaned “each” given its referent.v1.14: BEDROCK — the v1.13 remark box carried a false universal about Veltri. Bullet 2 said “his finality results are obtained assuming choice principles”, and bullet 3, added by v1.13, named the construction that refutes it: his preferred coinductive type (his Theorem 2) needs neither choice nor LLPO. The v1.13 fix added the counterexample and left the universal it contradicts standing one bullet above — the same half-sweep shape as v1.10's doubling. Bullet 2 is now scoped to the proofs that do use choice, and its duplicate LLPO-necessity clause removed. Also corrected in the Lean sources: Proposition 60 (iii) is separated from the CONSTRUCTIVE Proposition 59 (iii) by the SCOPE OF MAXIMALITY, not by the gamma <= beta bound, which both carry — v1.13 had asserted the inverse, an error imported verbatim from three round-3 reviewer notes and applied without checking it at the source.v1.13: gate debt cleared in one batch rather than carried as next-touch debt. The “not a necessity result” gloss was unscoped — it covered the LLPO half too, and LLPO is the one thing Veltri DOES prove necessary (p. 22:3 is an impossibility); scoped to the choice half. Also added his choice-free construction (ii) to the remark box: the Lean file carried that counterexample and the PDF did not, which is why the “all of” universal regenerated three times here — the box had no visible instance refuting it.v1.12: the v1.11 fix relocated its error for the THIRD time in one sentence — v1.9 said choice enters "only for" the finite-powerset functor (a universal), v1.10 PREPENDED a hedge and left the clause standing (a doubling), v1.11 removed the doubling and the hedge together and restored the universal as "are all of", which the same remark box contradicts two paragraphs up where it concedes that Mathlib's presentation of the POLYNOMIAL functor idPF_Coalgebra carries Classical.choice. The universal is now deleted rather than re-hedged. Also restored LLPO to the Worrell clause: Veltri's Conclusions and abstract both give construction (iv) as countable choice AND LLPO, and v1.11 had dropped it, leaving this document disagreeing with CLAIMS.md, which was right.v1.11: retrospective gate round on already-published text. The v1.10 hedge was PREPENDED and the original clause left standing, so the remark box read "In the cases the literature pins ... where the literature pins each presentation" — one clause doubled; rewritten once. Also retired the "genuinely enters" framing (remark-box heading and body): choice in the pinned finite-powerset presentations is a fact about those constructions, not a necessity, and Veltri's own preferred coinductive construction needs neither choice nor LLPO.v1.10: the v1.9 abstract fix RELOCATED its error rather than removing it — it made the theorem statement true but added a clause asserting that the framework’s own operators are monotone self-maps of a complete lattice, which the corpus explicitly denies and this document’s Section III calls open. Clause replaced by an explicit non-claim.v1.9: adversary review of the rendered document pre-publication — three substantive fixes: the abstract stated Knaster-Tarski WITHOUT monotonicity (false as written; the swap on the two-element lattice has no fixed point), the Tier-3 roster named the categorical INITIAL object as a contact point where Section II proves that object empty and locates the analog in the FINAL coalgebra, and quine_atom_unique was cited for two things it does not state.v1.8: swept the sibling v1.7 left standing — the next rendered paragraph still said the set-quotient and Worrell presentations REQUIRE choice, the modality v1.7 had just retired one sentence earlier; Veltri's own wording is "requires the presence of the axiom of choice in the proof of finality", about his proof rather than a necessity result.v1.7: the LLPO claim given its hypothesis — injectivity of the canonical algebra implies LLPO outright (Veltri Cor. 8, explicitly without countable choice) and is equivalent to it only under countable choice (Thm 9); the v1.6 changelog's own modality brought in line with the corrected prose.v1.6: CORRECTED a misattribution in rendered prose — choice-free constructibility of the final coalgebra FOR A POLYNOMIAL FUNCTOR was credited to Veltri (FSCD 2021) in the Lean-witness table and co-credited in the remark box; that is the wrong direction (his subject is the finite-powerset functor, which is NOT polynomial, and his results there are obtained ASSUMING choice principles rather than shown to need them — he hands the polynomial case to Ahrens-Capriotti-Spadotti himself). Now attributed to ACS (TLCA 2015), with Veltri explicitly marked as contrast; his LLPO claim, which IS his and IS about finite-powerset, unchanged.v1.5: rendered Lean citations synced to post-reorg files/namespaces (SSOT-driven).v1.4: rendered Lean-file citations synced to post-reorg basenames (ZPP_* -> *).v1.3: re-aimed the categorical-instance choice note per Veltri (FSCD 2021) literature review — the ν-side Classical.choice is a Mathlib M-type artifact, not a necessity (polynomial-functor final coalgebras are choice-free); choice genuinely enters only for the non-polynomial finite-powerset functor (full AC for the set-quotient; countable choice + LLPO for Worrell's limit); added "where choice genuinely enters" remark + Veltri/Ahrens citations (R1). C8 dual-date templating (Initial/Current meta line; hardcoded month removed).v1.1: Added categorical-parent instance as Lean witness (ZPP_Coalgebra.lean: fix_isEmpty μ-empty choice-free, cofix_nonempty ν-inhabited Classical.choice, categorical_fork_strict); set theory/computation referenced (ZP-J/ZP-K) not re-framed. v1.0: synthesis layer — abstract fork schema (lfp/gfp collapses iff unique fixed point) choice-free in Lean: fork_le, collapse_of_unique, unique_of_collapse, fork_collapse_iff [propext, Quot.sound]; number-system instance via Ostrowski: completions_exhaustive, real_not_equiv_padic [propext, Classical.choice, Quot.sound]. Generalizes ZFC+Foundation/AFA orthogonal-contact-point claim; three tiers (schema/instances/unification), hard fence (cross-instance identity = type boundary) + soft fence (not every fork is μ/ν). Lean: ZPP.lean, ZPP_Ostrowski.lean, ZPP_Coalgebra.lean — formal:4b914ac3 |
| ZP-R Cross-Category Fixed Point | v1.6 | ZP-R_Cross_Category_Fixed_Point.pdf | N/A | N/— | v1.6: BEDROCK - a wrong MECHANISM, not a wrong wording. The rendered text said the escape from the Cantor obstruction is that eval lands in the partial functions — "not in the codes, so the Set refutation never applied to it". False, and refuted in one line: the partial-function type is itself nontrivial, so `no_witness_of_nontrivial` forbids the Lawvere witness there exactly as on Code (`example : ¬ HasLawvereWitness (ℕ →. ℕ)` elaborates). Changing the codomain buys nothing. The real escape restricts which MAPS exist: no computable self-map on codes is eval-fixed-point-free (`no_computable_evalFixedPointFree`), so the diagonal the Set refutation runs has no computable representative. That theorem was already in the corpus with this mechanism in its own docstring; the prose asserted a different reason beside it. Adversary gate, FAIL-BEDROCK round 3; confirmed by elaboration before the fix, then swept across five sites and three surfaces at once rather than fixed at the one named. v1.5: Repaired a spliced sentence introduced by v1.4's Knaster-Tarski scoping fix — the clause landed between subject and verb, leaving "Nat.succ ... bans a fixed-point-free monotone endomap", which inverts why Nat.succ is cited. Mathematics and the Tarski 1955 attribution were correct throughout; only the sentence was corrupt. Found by two gates independently, both reading the RENDERED page. v1.4: Scoped the Knaster-Tarski claim. The monotone regime is free of the obstruction LITERALLY, but only on a COMPLETE lattice - the completeness hypothesis is load-bearing, and Nat.succ is the counterexample without it: a monotone endomap with no fixed point at all. (The scoping clause landed spliced between subject and verb; v1.5 repaired the sentence.) v1.3: CC-NAME-1 — the witness `computable_no_fixedpointfree` is renamed `computable_fixedpoint_up_to_eval`. The old name asserted the false strong form its own docstring refutes, and two prose gates independently named it the root cause of a claim that had to be corrected at four sites. ZPR-1 ORD-A: "free of the obstruction, for structurally identical reasons" was the same false claim in the opposite polarity — the monotone regime is free of it LITERALLY (Knaster-Tarski), the computable regime only UP TO EVAL. ⚠ The v1.0 note below deliberately keeps the OLD name: it records what v1.0 contained, under the name it contained it. v1.2: Corrected a false mathematical premise in the computability result box. "There is no fixed-point-free total computable endomap" is FALSE — c ↦ pair(c,c) is total, computable and fixed-point-free, both facts elaborated. Rogers gives a fixed point only UP TO eval, and the obstruction is absent at that level, which is where the reflexive object lives. The conclusion (the reflexive object is present) survives; the premise it was drawn from does not. v1.1: Corrected a source attribution. Lawvere (1969) does NOT derive the recursion theorem — his §2 p. 9 raises the recursive case as an open question; the derivation is Yanofsky (2003) Theorem 5, p. 18. Verified at source in both filed copies. v1.0: Initial release. Synthesis / placement layer — locates and realizes the framework's self-application fixed point ⊥ as a Lawvere fixed point across three faces: refuted in Set (Cantor; reflexive_object_refuted, not_monotone_not), obstruction-free but not a reflexive object in the monotone/domain regime (monotone_regime_derives_pinned; Scott D∞ unbuilt), realized in the computability face (eval_point_surjective, computable_no_fixedpointfree, selfref_fixedpoint_exists_computable — the crossing). R1 fork (fork_collapse_iff via instance_pinnable_iff_fork_collapse); R2a/b existence-as-selfApp + uniqueness-extra (lawvere_fixedpoint_selfApp, selfApp_pinnable, existence_without_uniqueness); R4 μ/ν = Lawvere regimes (mu_nu_branch_exclusion). Scope: existence-as-Lawvere (computability face) vs uniqueness + location (fork/AFA face: t_exec/t_exec_iff, quine_atom_unique, unique_fp) each proved but face-local and non-composable (F1/F2) — the computability face is non-unique (infinite_quine_family). Global identification held as a fenced conjecture. Choice-free spine, choice-carrying computability realization. Lean: RequirementsGap/MetaFork/LawvereBridge/SetTheoryAFA/SelfApp/ComputableCrossing/Kleene — formal:cfe3495f |
| ZP-R Diagonal Family Addendum | v1.1 | ZP-R_Diagonal_Family_Addendum.pdf | N/A | N/— | v1.1: ROGERS-1 — the up-to-eval qualifier. The document said "every computable self-map on codes has a fixed point", bare. That is FALSE as stated: c ↦ pair(c,c) is total, computable and literally fixed-point-free (both facts elaborated). Rogers gives a fixed point only UP TO eval — two codes computing the same function. The escape from the Cantor obstruction survives but for a different reason than the text gave: eval lands in the partial functions, not in the codes, so the Set refutation never applied. Found by the adversary gate 2026-08-17 after the same claim had been corrected three times elsewhere; this surface was missed because the sweep keyed on the DECLARATION NAME being renamed while a statement-identical theorem is cited under a different name (DC-24; the .lean-versus-.py diagnosis was falsified 2026-08-18). v1.0: Initial release. Addendum to ZP-R — the complete diagonal-family roster tied to ⊥, organized by the μ/ν fork. Supersedes the private ZP-W "Zero as a Wall" draft. Engine: negation_no_fixedpoint, lawvere_fixedpoint (Wall.lean; choice-free). Wall faces μ (self-reference cannot close): Cantor (cantor_via_engine), Russell (russell_via_engine), Turing (no_self_decider), the wall wf_no_selfloop — all Wall.lean choice-free; Tarski (tarski_no_internal_truth, Tarski.lean), Curry (curry_paradox/curry_no_bottom, Curry.lean). Floor faces ν (self-reference closes): Quine atom (t_exec/t_exec_iff, SetTheoryAFA.lean), Kleene quine (computability_face_fixedPoint, Lawvere.lean; choice-carrying), Löb + Gödel 2nd (loeb, godel_two, Loeb.lean; choice-free), Rice exists-but-undecidable (rice_face_has_bottom, quine_exists_yet_rice, Rice.lean; choice-carrying). Gödel 1st between the columns (via lawvere_fixedpoint). Unification = Lawvere 1969 / Yanofsky 2003 (cited); contribution = axiom-free core formalization (computability faces choice-carrying) + tie to ⊥ (placement, not new theorem); cross-face identity a type boundary (ZP-P hard fence / ZP-R F2) — formal:937a0e90 |
| ZP-Q The Frame-Change | v1.11 | ZP-Q_The_Frame_Change.pdf | N/A | N/— | v1.11: DA-1/KLEENE CLASS, GATE ROUND 4 (Tim rulings, 2026-09-15): the computability realization now says ZP-K carries the computational face as a KleeneStructure requirement, and that the unique fixed point of self-application is the AbstractSelfApp class field unique_fp. v1.10: NOVELTY OVERCLAIM RETRACTED (bedrock). Two sites attributed novelty to a NAMED theorem in the one chart where the corpus proves the opposite - the encodings converge to the floor they started from. v1.9: the same universal negative as ZP-P v1.24, corrected in one sweep rather than one site — the previous round fixed README alone and thereby manufactured a self-contradiction between two paragraphs eighty lines apart. No other claim changed. v1.8: Section III's computability sentence restated. It said the genuine Lawvere fixed point arises "where the fixed-point-free diagonal is not computable", which invites the reading that no fixed-point-free computable endomap exists - c ↦ pair(c,c) is one, total and computable. Now: the diagonal CONSTRUCTION is not admissible, removing the obstruction does not by itself supply the witness, and the fixed point is cited (Rogers, up to eval) rather than derived. No claim changed; recorded because the bump shipped without an entry. v1.7: VOCABULARY DECISION, recorded rather than deferred. "instance" was doing two jobs - the technical instance-of-a-theorem relation, and ZP-P's inherited tier-2 word for a per-domain realization - so after v1.6 answered the universal question in the negative, page 1's title-block note and page 7's endnote gave opposite answers about the same two objects purely through that collision. "instance" is now reserved for the instance-of-a-theorem relation; a per-domain realization is a "realization" or a "face". Five rendered sites; no claim changed. The Lawvere-instance uses in Section III are the technical sense and stay.v1.6: the v1.5 fix was UNSWEPT (adversary round 5). v1.5 corrected Section I to say NONE of the min=max facts is an instance of fork_collapse_iff and left Section III and the endnote byte-identical to v1.1, where Section III answered its own headline question - "one theorem all the instances are cases of?" - in the AFFIRMATIVE for catseam_is_frameflip, the object Section I rules out by name, and the endnote called it an "instance" outright: one PDF asserting and denying the same proposition eight pages apart. Section III now answers in the negative and keeps the true half (the theorem is universal over complete lattices; the faces fail its hypotheses); the endnote says "face". The denial framing ("they were never the same map", "the snap is a separate object") is replaced by the conjectural form per the POV KIND/STATUS convention - the identification is held OPEN, not settled either way.v1.5: the v1.4 fix was half-applied - the rendered text still called selfApp_bot_is_both_extremal and the categorical seam 'genuine instances' of fork_collapse_iff. None of the framework's min=max facts is an instance (it needs a complete lattice and a monotone map); they share a shape, which across distinct structures is a type boundary.v1.4: struck the eps0 half of the same instance claim.v1.3: rendered SUBTITLE still asserted the identification v1.2 struck.v1.2: BEDROCK framing - the layer identified the SNAP with the frame-change; the Lean does not. The frame-change is the POLE EXCHANGE (bottom read as both 0 and infinity); the snap is one covering step off the zero face (AX-B1, a commitment). The snap-as-instance reading remains this layer's conjecture.v1.1: frame-change prose precision (tower encodings converge to ⊥ / diverge to ∞; ε₀ never "realised as ⊥/the ceiling", ε₀ ≠ ⊥; theorems unchanged). v1.0: Initial release. Synthesis layer, ZP-P sequel — ⊥→ε₀ as a change of point of view. §I frame-flip schema (fork_is_frameflip, ForkFrameChange.lean; choice-free [propext, Quot.sound]); §II per-domain instances (valuation/Riemann sphere: snap_is_frameflip / RiemannSphere.lean; category: catseam_is_frameflip; set-theory/computability/Hilbert/information referenced; reals NO-GO); §III universal + walls (order-theoretic universal holds; the categorical Lawvere universal meets a proven wall, Cantor/Lawvere.lean; cross-domain identity a type boundary). Three tiers; "the fence is the bridge"; the bottom never positively represented; static Riemann-sphere figure. formal:dcf69119|
| Zero Paradox Foreword | v2.26 | Zero_Paradox_Foreword.pdf | N/A | N/A | v2.26: ZPK-BED-1 SECOND HALF, BEDROCK IN A DEPOSITED PDF (Tim ruling, 2026-09-19). The foundations paragraph said ZP-K's choice dependency is "the same dependency carried by any theorem using Mathlib's computability library". Refuted: `Nat.Partrec.Code` and `Nat.Partrec.Code.eval` are both axiom-free, `IsKleeneFixedPoint` measures no axioms, and a theorem that runs a program carries no choice. It now states the inheritance and POINTS at ZP-K § IV. ⚠ This site was not in the defect row's enumeration and was not found by the first gates — it came from an axis-varied claim sweep over the rendered PDFs, which is why this work stopped enumerating sites from prose. v2.25: ATTRIBUTION, PRIOR-ART GATE (PA-1, 2026-09-15): section IV credited Lawvere's fixed-point theorem (1969) with unifying Cantor, Russell, Gödel "and Kleene's recursion theorem". Lawvere does not treat it; his introduction names Cantor, Russell, Gödel and Tarski. Lawvere's list now ends at Tarski's undefinability theorem, and Turing's halting argument and Kleene's recursion theorem move to Yanofsky (2003). Same defect as ATTR-2 (README, closed 2026-08-29), whose no-further-instances claim is retracted. v2.24: DA-1/KLEENE CLASS AND MC-1 PREMISE, GATE ROUND 4 (Tim rulings, 2026-09-15): section IV reads 'It proves the lattice, 2-adic and categorical faces in their own domains and carries the computational one as a requirement'; 'In computation it is read as the self-reproducing program'; the MC-1 pointer carries 'in a lattice with no top'. v2.23: DECISION BATCH REMEDIATION AFTER GATE ROUND 3 (Tim ruling, 2026-09-15): section IV 'The framework reads them as faces of one object' is past tense, 'read', since the next paragraph retires that reading. v2.22: DECISION BATCH REMEDIATION AFTER GATE ROUND 2, SECOND PASS (Tim ruling, 2026-09-15): section IV no longer says the framework proves several faces literally identical; it proves each face in its own domain, and that they are one object is the reading the next paragraph retires. v2.21: DECISION BATCH REMEDIATION AFTER GATE ROUND 2 (Tim rulings, 2026-09-15): MC-1 identity retired because object equality across categories does not typecheck and is not invariant under equivalence; the commitments-table row keeps the property clause scoped to the named bottoms, the section IV paragraph keeps only the retirement. v2.20: DECISION BATCH REMEDIATION (Tim, 2026-09-15): MC-1 row and the self-reference paragraph no longer say the members are 'provably distinct (the walls)' or that 'the walls between its members are themselves theorems': the identity and its negation are retired as ill-typed, and the members differ by property, each in its own category (seam_unique_among_named). v2.19: OCCURRENCE COMMITMENT DEFINED, T5 RESTATED, T-SNAP RESIDUE (Tim decision batch, 2026-09-14): the commitments-table AX-1 row says that the Snap occurs follows from the occurrence commitment (instantiation occurs) together with DA-1 (closed given DP-2), with no Lean kernel axioms on the shape side; the ZP-E layer paragraph now carries the shape/occurrence split. v2.18: AX-1 RETIRED, CONTENT SPLIT (Tim, 2026-09-14): the commitments table row "Retired axiom → Theorem T-SNAP" credited T-SNAP with all of AX-1; it now says AX-1 is retired, its shape proved as Theorem T-SNAP and its occurrence stated separately as the occurrence commitment. v2.17: CC-1 STATUS SYNC (2026-09-13): "CC-1 derived" collapsed two readings; cc1_derived proves the conditional (a Quine-atom start is a ⊥ start) and t_exec_iff the converse, so the starting-point choice is restated through the Quine-atom role, not forced. The ZP-J summary also follows the CC-2 role convention. v2.16: OCCUPANCY OVERCLAIM RETRACTED, and a WALL PRESENTED AS A GAP (bedrock). Neither site was reached by the ZP-I v1.16-v1.18 arc, though this is a general reader's first contact. (1) "converges, at its limit, to something that fills the bottom role again" asserted OCCUPANCY as reached; T-IZ proves convergence to 0 in Q2, and that the limit is a thing filling the bottom role is a COMMITMENT - not merely unproved, since ZPSemilattice Q_[2] does not synthesize and the join-identity is not statable of the limit. The proved half, anything filling the role IS the bottom already there, is kept. (2) "whether they are all one object in the deepest sense remains an identification we make rather than a theorem we have closed" presented a WALL as an open question: equality across distinct categories is not well-formed, the identity is retired as ill-typed, and the members are PROVABLY DISTINCT. v2.15: NOVELTY OVERCLAIM RETRACTED (bedrock). A general reader's first contact said ZP-I "proves" that the accumulation "produces a new bottom". T-IZ gives convergence plus the ROLE identification; novelty is C-DA2, a commitment. v2.14: "Rogers' fixed-point theorem" corrected from "Roger's" (Hartley Rogers Jr.). ZP-L made this exact correction at its v1.4 and it was never swept to the rest of the corpus; Mathlib carries the same typo upstream at Computability/PartrecCode.lean:36,1001. Prose only, no claim changed.v2.13: T-COMP overclaim corrected (bedrock) - four-way -> three-way, Kleene's fixed point named as a KleeneStructure assumption not a fourth clause; "DA-1 is closed concretely ... grounding the framework in the theory of computation" -> structural half only, self-execution a commitment. v2.12: SYNC TO CLAIMS.md + bedrock false-premise fix (release-prep) — struck "a well-founded ⊥ would admit an external interpreter" (finite-interpretability fallacy, same class as ZP-E v3.24 / ZP-A v1.20), ZFC-incompatibility now on ⊥={⊥} self-membership (no_quine_atom); AX-B1 = the one substantive modeling commitment (not "directly verifiable / not a novel commitment"); MC-1 = the bottom family (not a commitment; identity retired ill-typed), CC-1 derived (ZP-J cc1_derived), CC-2 a Forced Metatheoretic Commitment.v2.10: §IV cited Yanofsky (2003) + Lawvere (1969) for the diagonal-fixed-point unification (prior-art positioning; paired with the new CLAIMS "Convergence with established work" section). C8 dual-date templating (Initial/Current meta line; hardcoded month removed).v2.8: FMC uniformity — CC-2 metatheoretic row "Foundation is ruled out" → "argued to be ruled out". v2.7: §IV names Lawvere's fixed-point theorem as the recognized unification of the diagonal family (Cantor/Russell/Gödel fixed-point lemma/Kleene); fence firmed — ZP "locates" the diagonal fixed point, not "is". v2.6 added the keystone — formal:a1b6072d|
| ZP Philosophical Question | v1.21 | ZP_Philosophical_Question.pdf | N/A | N/A | v1.21: DECISION BATCH REMEDIATION AFTER GATE ROUND 2 (2026-09-15): three sites saying the Snap being taken is a commitment alone now say it follows from the occurrence commitment together with DA-1 (closed given DP-2). v1.20: DECISION BATCH REMEDIATION (Tim, 2026-09-15): the opening's 'That the snap occurs is a commitment of this framework, not one of its theorems' now says it follows from the occurrence commitment together with DA-1 (closed given DP-2). v1.19: OCCURRENCE COMMITMENT DEFINED, T5 RESTATED, T-SNAP RESIDUE (Tim decision batch, 2026-09-14): the layer summary carries the canonical AX-1 sentence with "no Lean kernel axioms"; the opening no longer attributes T-SNAP's shape to the bottom element axiom alone (A4 gives the join, AX-B1 the atom, and the distinctness is t_snap_given's first-step hypothesis). v1.18: AX-1 RETIRED, CONTENT SPLIT (Tim, 2026-09-14): the layer summary said T-SNAP closed "what had been the framework's one axiom (AX-1)"; it now says AX-1 is retired, the shape proved as T-SNAP and occurrence stated separately as the occurrence commitment, matching the paragraph after it. v1.17: OCCUPANCY OVERCLAIM RETRACTED (sync with ZP-I). "ZP-I proves T-IZ - every maximal ascending chain converges to a limit that fills the bottom role" asserted OCCUPANCY as part of what is proved and fenced only the successor reading beneath it. Convergence to 0 is the theorem; reading the limit as an occupant of the bottom role is a first commitment and reading that occupant as a successor null is a second. ZPSemilattice Q_[2] does not synthesize, so the join-identity is not statable of the limit. v1.16 retracted the novelty conjunct of this same sentence and left the occupancy conjunct standing, and this site was never in that arc's scope. v1.16: NOVELTY OVERCLAIM RETRACTED (sync with ZP-I v1.16). 'every maximal ascending chain converges to its own successor null' rendered the successor reading as part of what T-IZ proves; T-IZ gives the role identification, and reading that occupant as a NEW bottom is a commitment. v1.15: FORCING OVERCLAIM RETRACTED. The document asserted that T-SNAP establishes the snap OCCURS; it does not. T-SNAP fixes the transition's shape, and Order/Snap.lean's NO-GO gauge tsnap_holds_but_nothing_moves proves T-SNAP holds in a model where nothing moves. Occurrence is a framework commitment. Prose only. "You cannot avoid it" asserted occurrence; the surrounding derivability claim (the snap was an axiom, now a theorem) is correct and kept.v1.14: rendered layer COUNT eliminated (project-wide counter removal - "Thirteen formal layers"/"Thirteen layers" de-counted); stale "ZP-A through ZP-M" range dropped from body + endnote.C8 dual-date templating (Initial/Current meta line; hardcoded month removed).v1.11: fix() guard via Paragraph override; "bridge" → "connecting argument"; "in ZP's reading" de-duplicated — formal:ed65d90b|
| ZP Choice-Free Core Addendum | v1.10 | ZP_Choice_Free_Core_Addendum.pdf | N/A | N/A | v1.10: DECISION BATCH REMEDIATION ROUND 2 (Tim rulings, 2026-09-15): T-SNAP's occurrence no longer 'stays a framework commitment': the Snap occurring follows from the occurrence commitment (instantiation occurs) together with DA-1 (closed given DP-2). v1.9: the page-1 overview said § III shows it *essential rather than INHERITED* while § III itself says both cases are essential and one of them IS inherited — one document, opposite framings, the overview misdescribing the section it cites. Now *essential rather than INCIDENTAL*, which is the axis the sentence is on. **Found by `check_paths.py --claim`**, built this session as DC-24's mechanical half, on its first real run. v1.8: PROVENANCE and NECESSITY are independent axes and v1.5–v1.7 collapsed them. The text said § III locates two principles "where the choice is the framework's own and provably essential" — false for the second: `em_of_wellOrder_comparable` reduces well-order comparability, whose choice is MATHLIB's (spent in `InitialSeg.total`), and `OrdinalChoiceEssential.lean` states that use is forced. **So an INHERITED dependence can be essential** — stronger than the claim the prose was making, and the reason "inherited" never meant "removable". Found by keying on the POSITIVE assertion; four earlier rounds keyed on the universal negative and no search in that polarity could reach it. v1.7: the v1.6 fix reached the endnote and missed the FRONT MATTER — page 1 and the Section II opener still carried the universal, listing category theory as an inherited layer. Fourth consecutive version of this document fixing one site of one claim. Both prose gates FAIL-BEDROCK independently. Also struck a claim v1.6 INTRODUCED: that the axiom-free sibling shows the choice is not forced by the shape of the result — the inversion of `LawvereTaboo` § III, where the cost IS the generality over arbitrary types and vanishes under `[DecidableEq β]`. v1.6: THE ENDNOTE CONTRADICTED SECTION III OF THE SAME DOCUMENT. § III has named both taboo reductions since v1.5; the endnote still said choice “appears only where the framework builds on Mathlib’s … libraries, and whether it is necessary there remains open” — both halves false, in the two places a skimmer lands. This is the ZP-Q v1.6 shape (one PDF asserting and denying the same proposition) recurring in a document whose subject IS the choice footprint. Found by sweeping the CLAIM after both prose gates returned FAIL-BEDROCK on the same universal in README. v1.5: BEDROCK — Section III asserted that the framework has NO PROVEN-NECESSITY CASE ANYWHERE, a universal negative that is FALSE and was live in the published PDF. Two taboo reductions exist: em_of_wellOrder_comparable (comparability of well-orders implies excluded middle; prior art Kraus-Nordvall Forsberg-Xu arXiv:2104.02549 Thm 38(d)) and wem_of_fixedPointFree (the general fixed-point-free principle implies WEAK excluded middle, on the keystone). Neither was named anywhere in this document (0 rendered hits for either). A 2026-08-01 sweep recorded both universal negatives as removed from the corpus — it grepped .lean and missed this Python build script, so the claim survived in RENDERED PUBLIC PROSE. Found 2026-08-03 by the new modal-claim sweep (CLAUDE.md, Prose that resists correction is a CLAIM defect). Section III now states the reduction-vs-measurement distinction, names both cases, and adds the type-vs-proof point: an axiom in a TYPE cannot be removed by any proof, so removability there means changing the statement. Docstring header said 1.3 while VERSION said 1.4 — corrected.v1.4: FORCING OVERCLAIM RETRACTED. The document described the snap as forced with no occurrence hedge. T-SNAP fixes the transition's SHAPE; tsnap_holds_but_nothing_moves proves it holds in a model where nothing moves, so occurrence is a framework commitment. Prose only. NOTE: this row also carried a STALE hash token that no gate could catch - check_hashes.py had no key for this document (nor for the three ZP-J addenda). All four are now guarded.C8 dual-date templating (Initial/Current meta line; hardcoded month removed).v1.3: rendered Lean-file citations synced to post-reorg basenames (ZPx_* -> *).v1.1: WheelFrac.* citation → ZPJ_WheelFrac.* (Lean namespace standardization). v1.0: Initial release — the conceptual core is choice-free; T-SNAP axiom-free; Classical.choice confined to the analytic realizations (inherited from Mathlib); dependence ≠ necessity (open). Anchored on AxiomProfile.lean — formal:1834c1f5 |

**Comp AR column key:** `Y/Y` = current comp hash adversary-reviewed + remediated (or confirmed clean). `Y/N` = reviewed, fixes identified but not yet applied. `N/—` = not yet reviewed.

---

## Script Hash Verification + AR Tracking

The `formal:XXXXXXXX comp:XXXXXXXX` tokens in the Notes column above are SHA-256 (first 8 chars) fingerprints of the corresponding build scripts in [`scripts/`](scripts/) — public and tracked, so a reader can recompute them. They moved there on 2026-08-15; the tokens were recomputed from the normalised (LF) bytes at the same time, because a fingerprint of one machine's CRLF working copy is not provenance. The `Comp AR` column tracks adversary-review status for each companion, backed by `.claude-local/ar_status.json`, which is private — its absence is reported as unavailable, never as a mismatch.

**Session start** — run once before touching any build script:
```
python tools/verify/check_hashes.py
```
A hash `MISMATCH` means a script was modified without a version bump and PDF rebuild. An AR status of `STALE` means the companion script changed since its last adversary review — re-review required before merge.

**Post-fix workflow** — after applying adversary-review fixes, rebuilding the PDF, and updating the hash in register.md, run one command to close the loop:
```
python tools/verify/check_hashes.py --mark-remediated ZP-X
```
This computes the current comp hash, writes it to `ar_status.json` as remediated, and updates the `Comp AR` column in this file automatically. For fixes identified but not yet applied, use `--mark-reviewed ZP-X` instead (sets Y/N).
