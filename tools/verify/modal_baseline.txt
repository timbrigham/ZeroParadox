# check_modal.py baseline - grandfathered sites, each verified by READING it, not by counting.
#
# ZeroParadox/Ordinal/Kruskal.lean (x2) - the `artifact` sites are GONE as of 2026-09-03 and the
# entry below is INERT. Kept rather than removed: removing a baseline entry owes a /claim-review,
# because no checker can tell content GONE from content MOVED.
#   ⚠⚠ THIS NOTE SAID "NOT a defect" AND THAT WAS THE DEFECT (KRU-1, fixed 2026-09-03). It held
#   the eliminability claim supported "by EXHIBITING the clean proof". None was exhibited:
#   Coq-Kruskal proves the almost-full formulation, Kruskal.lean proves the SEQUENCE form, and
#   transport needs a direction CCC2017 records as intuitionistically invalid. Now UNMEASURED.
#   It also called the choice located "precisely" at Mathlib's minimal bad sequence machinery;
#   three further routes carry it, two of them written in that file, and its PurityCheck block
#   now EMITS all four rather than describing them. Read the file, not this note.
#
# Baselined rather than widening EVIDENCE: a pattern broad enough to auto-pass this would also
# rubber-stamp the identical sentence asserted WITHOUT any citation, which is precisely the defect
# this gate exists to catch.
#
# SHRINK this file as files are touched; never grow it deliberately. Format: <path>|<phrase>
ZeroParadox/Ordinal/Kruskal.lean|artifact

# .claude/commands/claim-review.md - NOT a defect. The hit is a SECTION HEADING that NAMES the
# defect class the gate looks for: "3. Eliminable vs. necessary (the method-reach asymmetry)."
# It does not assert that anything is eliminable; it instructs a reviewer to catch someone who
# does. This is CLAUDE.md's `Idiom:` case - prose that NAMES a phenomenon rather than USING the
# claim - and the test it gives applies cleanly: does the sentence USE the claim, or NAME
# something? It names. Grandfathered rather than reworded: contorting a gate brief's own
# vocabulary so it stops matching its own subject would make the brief worse to read, and the
# checker cannot distinguish mention from use.
#
# Became visible 2026-08-16 only because check_modal's markdown scope was widened from a
# root-only glob to `git ls-files`; the file was never scanned before.
#
# ⚠ DO NOT REGENERATE THIS FILE WITH `--baseline`. Measured 2026-08-16: it rewrites the file
# from current state and DISCARDS every comment here - 26 lines to 5 - destroying exactly the
# per-site reading notes this file's own header requires. Add a site by hand, with its reason.
.claude/commands/claim-review.md|eliminable
