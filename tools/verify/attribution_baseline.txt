# check_attribution baseline — ADD BY HAND, with the reason on the line.
#
# A baseline is DEBT, not a decision. It blocks on NEW sites only. Every entry below was READ at
# the artifact before being written here; none was regenerated. Regeneration would grandfather
# sites nobody read, which falsifies the one property that makes a baseline worth anything.
#
# Format: <file>:<Author>:<verb>
#
# ⚠ A DECLARED LIMIT, and it accounts for three of the four entries. This checker cannot tell
# "Lawvere" the PAPER from `lawvere_fixedpoint` the corpus THEOREM. Prose about the framework's own
# machine-checked declaration reads exactly like prose about the source it is named for, and only a
# human knows which is meant. Where the subject is the corpus's own theorem, no page locator exists
# to give.

# The subject is the corpus's own `lawvere_fixedpoint`, not the 1969 paper: this compares what the
# THEOREM yields (existence) against what the framework adds (uniqueness). No page applies.
ZeroParadox/Settheory/LawvereBridge.lean:Lawvere:gives

# Same — describes what the corpus's engine does at `Not`, producing the Russell element. A page
# number for the paper would be the wrong citation here.
ZeroParadox/Settheory/Wall.lean:Lawvere:gives

# The locator is a FORMAL IDENTIFIER, which is stronger than a page: `not-finite-is-∞'` in
# TypeTopology's `GenericConvergentSequence`. A reader can `#check` it; a page number would rot.
CLAIMS.md:Escardó:proves

# An H1 TITLE, not a claim. The body carries the citation properly — Carlström Prop. 4.4, p. 27 —
# and a title that repeated the locator would be worse to read.
ZeroParadox/Algebra/WheelFrac.md:Carlström:proves
