{
  "schema": "zp.policy.v1",
  "_comment": "CONFIG, NOT CONSTANTS. If changing a policy means editing logic, it is in the wrong place. Every value the process compares against lives here; how it compares lives in code. A malformed file makes the ledger serve UNDECIDED and refuse every gated action -- it must never fall back to a built-in default, because a built-in default is a second copy of the policy and the weaker one is the copy nobody notices.",
  "agreement": {
    "_comment": "V3 says agreed == passes AND passes >= min_passes. Three was a decision, not a law, so it lives here. Change it and the verdict changes with no restart and no code edit -- that is the behavioural proof the threshold is data.",
    "min_passes": 3
  },
  "supersede": {
    "_comment": "V13. `revision` is an ordinal scoped to one (step, basis); a chain never crosses bases. At the cap, refuse AND surface it: regraded to the cap means the step or the subject needs fixing, not another regrade.",
    "max_depth": 5
  },
  "convergence": {
    "_what": "THE FROZEN BAR for the convergence run that started 2026-08-29. Scope lives in the type registry, so freezing scope means freezing required.v2.json -- this is its sha alone, NOT the composite config_sha, so raising a threshold does not read as a scope change and the freeze does not cry wolf.",
    "_why": "Tim, 2026-08-29: 'no more random ass stuff getting into scope at a later point.' A widened scope does NOT re-open a green row, because coverage is keyed on CONTENT rather than on the bar -- so scope can move mid-run and reset progress invisibly. Measured the same day: 8 of 12 green steps had earned their green under a registry that no longer existed, and nothing had surfaced it. `progress` now compares this value on every call.",
    "_release": "Clear this key (or re-freeze to the new sha) only DELIBERATELY, when the run reaches 19/19 or when a scope change is worth restarting the count for. The pending *.html widening for editorial/adversary is a real finding and lands AFTER, not during.",
    "_refrozen_2026_09_01": "RE-FROZEN under the first clause of `_release`, at 19/19 satisfied and complete. Tim's decision, asked and answered before the edit rather than after. What moved the bar: `types.rely.scope` went from ['tools/verify/*'] to all THREE prefixes batch.py's ROUTING sends to /rely. That was a CORRECTION of a scope that was wrong, not a widening -- `tools/process/` was exempt from editorial and adversary ON THE STRENGTH OF /rely covering it while /rely's scope contained none of its files, and `.github/workflows/` was routed and unscoped the same way. ⚠ The bar is only worth re-freezing because the divergence that caused it can no longer recur silently: guards.py PROPERTY 'the registry scope and the router agree' now derives the obligation from batch.ROUTING and BLOCKS, with four controls including both live instances. Previous bar: eb096f62 (frozen 2026-08-29, held until this edit -- nothing else moved it).",
    "_refrozen_2026_09_01b": "SECOND re-freeze the same day, and the reason it is not a habit forming: the `prior_art` entry's DESCRIPTION had to stop being false. Its text said the obligation was DISCIPLINE where it had briefly been a gate; as of this commit it is a BLOCKING push leg again (`batch.check_prior_art_attribution`), so leaving the old wording would have left the registry asserting the opposite of what the pipeline does. `actions` and `scope` are unchanged from the previous freeze -- the promotion attempted between these two freezes was measured, found backwards, and reverted in full. Only prose moved.",
    "frozen_registry_sha": "9e904de1d38a248e6f8f109ef0645676f07bb0baee7fbb71ae784586062d54d8"
  },
  "lock": {
    "_comment": "The bounded wait. Appends are sub-millisecond, so crossing soft can only mean a stuck writer or a stalled fsync, never contention. WARNING: hard_seconds is the process supervisor's poll interval, and a wait that long is safe ONLY because every MCP tool is async-offloaded so the health endpoint keeps answering. Make one tool a plain def and this trips the supervisor precisely.",
    "soft_seconds": 5,
    "hard_seconds": 30
  },
  "actions": [
    "commit",
    "push",
    "tag"
  ],
  "signals": {
    "never_fired_runs": 50,
    "disagreement_low": 0.05,
    "disagreement_high": 0.3,
    "repeat_subject_rounds": 3
  },
  "genesis": {
    "_comment": "THE FLOOR IS NOT SET HERE. It lives in the stream as a `genesis` RECORD, seeded with `zpledger genesis <sha>`, because it is a claim about when RECORDING began and belongs somewhere append-only, validated and attributable -- not in a file anyone can edit without a trace. An earlier version of this block told readers to set `commit` here and nothing consumed it. It is a fact about when recording began, NEVER a claim that earlier work was verified."
  }
}
